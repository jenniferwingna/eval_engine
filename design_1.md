一、整体架构
1.1 架构全景图




二、Agent Loop :流式驱动的循环引擎
2.1 Agent Loop 流程图





2.3 错误恢复机制
与BBP 方案对齐，直接继承现有的错误码处理。

三、Tool 调用机制

3.2 工具分类
分为11大类、53个叶子小类
大类：车辆控制、驾驶、能量、显示、声音、连接、系统、系统应用、车辆状况、情景模式
车控  API 总结表 — 大类 / 小类 / 叶子数
大类
大类API总数
小类（叶子）
小类API数
占大类比例
车辆控制
233
座椅与头枕
27
11.6%
后视镜
25
10.7%
车窗及自动化
16
6.9%
车外主照明与雾灯
16
6.9%
氛围与装饰灯
14
6.0%
座舱环境
13
5.6%
车外信号与灯语
13
5.6%
车顶与遮阳
12
5.2%
舱内功能照明
12
5.2%
车辆解闭锁
11
4.7%
车门
11
4.7%
储物舱门
11
4.7%
车外迎宾与状态灯效
11
4.7%
方向盘设备
7
3.0%
冷暖箱
6
2.6%
雨刮
6
2.6%
能源口盖
5
2.1%
车身外部执行件
4
1.7%
中岛与杯托
4
1.7%
香氛
4
1.7%
儿童锁
3
1.3%
自动寻车
2
0.9%
驾驶
71
驾驶与路况模式
23
32.4%
行车辅助与主动安全
12
16.9%
加速制动与转向
12
16.9%
悬架
10
14.1%
泊车与环视
8
11.3%
乘员监测与安全
5
7.0%
特殊车身运动
1
1.4%
能量
34
充电与供电
17
50.0%
动力驱动与能量回收
11
32.4%
能源策略与续航
6
17.6%
显示
49
其他后舱与远端屏幕
11
22.4%
幕布与投影
8
16.3%
HUD
7
14.3%
显示个性化与维护
6
12.2%
吸顶屏
5
10.2%
中控屏
4
8.2%
仪表屏
4
8.2%
副驾屏
4
8.2%
声音
35
音效与声场
13
37.1%
音频控制策略
12
34.3%
提示与告警音
10
28.6%
连接
13
无线连接与ETC
13
100.0%
系统
28
系统页面导航
15
53.6%
用户身份与驾驶档案
8
28.6%
通用交互
5
17.9%
系统应用
15
摄像与相册
8
53.3%
系统操作与维护
7
46.7%
车辆状况
12
车辆维修与保养
8
66.7%
整车状态与控制
4
33.3%
情景模式
26
座舱体验场景
26
100.0%
语音助手
18
语音助手
18
100.0%
3.3 意图识别
3.3.1 意图大类识别
从Intent Head 大类选取候选： 546 -> k
模型返回大类意图（e.g Climate），编排层对类目进行读取：
e.g. 用户：“打开车窗然后播放音乐”
模型调用：
system prompt : 
你是车载语音助手，请简洁回答。
{能力版本戳 i.e. 本轮基于那个车型能力快照}
{模型输出格式}
{20个能力大类的语义说明：每个大类的中文标签及一句话描述}
{User Query}
只输出一行，不要解释。格式是：意图标签列表、完整转写、每个意图的执行模式、每个意图的前置依赖。 
意图标签只能从这 22 个里选（20 个能力大类 + 对话 + 导航子代理）。 执行模式只能是：执行 / 查状态 / 闲聊 / 子代理 / 条件触发 / 被否定。 
转写必须保留完整原话，禁止切分。 
同一个大类里的多个操作只算一个意图，具体拆成几个调用由下一步决定。 （语义目录已经在音频前给过了，这里不重复。）
模型返回：
§plan {"intents":["doors_windows","audio"],
       "transcript":"打开车窗然后播放音乐",
       "modes":["call","call"],
       "deps":[[],[1]]}§
模型输入：
拿到计划表后，系统才去加载 schema——只加载Intent的那 2 个大类的，不是全部 20 个。
然后把这些 schema 作为"工具反馈"注入上下文，要求模型生成具体 API 和参数。
【task task-1】  转写="打开车窗然后播放音乐"  大类=doors_windows  模式=call
  1. car_door_electric_mode  控制车门的电动模式开关   参数：switch(枚举)、position(枚举)
  ...
  29. window_switch          明确打开或关闭车窗        参数：switch(枚举)、position(枚举)
  ...（共 31 项）

【task task-2】  转写="打开车窗然后播放音乐"  大类=audio  模式=call
  ...
  8. media_play              控制音乐播放              参数：action(必填, 枚举)、track(可选)
  ...（共 26 项）
模型返回：
§calls {"task-1":[["window_switch",{"switch":"open"}]],
        "task-2":[["media_play",{"action":"play"}]]}§

3.4工具编排：并发策略

【@暂定并发策略】
维护一个task agenda, 由 Done Doing Todo三部分组成
我大概画了一个流程图 是有些问题 比如 没有事件管理 去细化和检查依赖项 例如在do taks 前应该加三个框 “检查并根据依赖调整agenda TODO顺序” DO DASK框后面加三个框 比如说调整agenda 把doing 挪到done 异步事件和播报接受等：
user query -> mola omni模型 -> task agenda -> do task_i -> all tasks done?  -> if yes , announce
-> if no , back to task agenda
3.4.1 进批任务检查
Gate 1: 依赖就绪
任务的所有前置依赖必须全部成功。只要有一个前置失败，该任务立刻标记为"因依赖失败而失败"，不再尝试。
依赖关系来自第一跳模型给的 deps，并且协议强制只能引用更早序号的任务——所以依赖图在构造上就不可能成环。之外还会再跑一遍完整的图校验（重复 ID、自依赖、悬空边、环）。
Gate 2: 资源互斥
每个调用都携带一组"资源锁"。选批时按计划顺序贪心：如果一个任务需要的锁已被本批其他任务占用，它就顺延到下一波。
如何上锁？ 分两阶段：
首次模型交互（粗粒度）：锁就是意图大类本身，比如 group:doors_windows。协议强制每个执行类任务锁自己那一个大类。对话类、导航子代理类、条件类、否定类任务必须不锁任何资源
第二次模型交互（细粒度）：换成 工具集 : 物理区域。区域从已校验的参数里取（主驾/副驾/后排等），没指定就是"全车"
实际效果：
用户说
锁
结果
打开主驾车窗和副驾车窗
同工具集，不同区域
同一波并行
把车窗打开再关上
同工具集，同区域
强制拆成两波
打开车窗、播放音乐
不同工具集
同一波并行
锁的 ID 永远由确定性代码从静态分类树和已校验参数推导，绝不采信模型输出的自由文本。 模型可以决定"做什么"，但不能决定"什么和什么可以同时做"。
任务失败处理
一个任务失败，只有它的下游会被连带标记失败；不相关的分支照常跑完。
用户打断
所有在途任务被取消，所有"待办"和"进行中"的任务统一标记为"被中断"，并保证这个终态一定被推送到前端让用户感知（比如 打开车窗已取消），不会因为取消而丢事件。
3.5工具执行
3.5.1 分解出 Tool Call
输入：
§call window_switch switch open§
过程：
ACT_RE = re.compile(r"§?\s*(state|intent|asr|call)\s+([^§]+?)\s*§")
输出：
name = "window_switch "
args = ["switch", "open"]
3.5.2 校验与参数校正
3.5.2.1 检查是否在大类中，并生成回执
模型吐    §call conditioner_power on§
             ↓  conditioner_power 不在 climate 那 18 个里
硬闸拦下,回一张失败回执:
    { "结果": "失败",
      "原因": "conditioner_power 不属于当前闭集大类 climate",
      "当前大类工具": ["conditioner_switch", "conditioner_mode", …],
      "现在必须做": "只从当前系统给出的 Tool Schema 原样选择并重试。" }
             ↓  这张回执写进模型的 KV
模型再生成一次   §call conditioner_switch on§   ← 多跟模型交互一轮，回执将带着失败信息和当前大类的列表重新做选择

*重试次数 = 10 ； max_Hops = 10 多交互一次就会占一次跟模型交互的次数，
3.5.2.2 四道改写
规则
定义
例子
名字重写（存在风险）
当出现：
1. 原api在546个工具表里查不到；
2. 替换后，确实存在在表里
3. 第一个参数是`on/off/open/close；

就触发改写，直接把最后一个下划线后的词汇替换为 “_switch”
e.g.
`§call conditioner_power on§` → 改为 `conditioner_switch`
`§call conditioner_power set 23§` → 不改（参数不像开关），丢弃
位置检验
查看当前会话中，声音传输的真实位置
模型吐  §call door_switch off driver§
改写成  §call door_switch off secondRowLeft§
空参兜底
如果模型没有成功提取参数，规则会去用户原话进行提取，条件是：
1. 在这个工具自己的枚举里
   比如 fridge_mode的valid取值是：制冷、制热、保温等
   “把冷暖箱打开，制冷” -> call fridge_mode 制冷 on
2. 字面出现在原话里
用户说「把冷暖箱打开，制冷」
模型吐  §call fridge_mode on§ 参数是空的
改写成  §call fridge_mode on 制冷§
中英归一
有一个映射表进行转换：
(auto→自动、dark→深色、cool→制冷…)换过来。换完还要确认换出来的词确实在这个工具的枚举里才替换 —— 否则 on/off/set 这些协议词会被误翻

模型吐  §call fridge_mode on cool§
改写成  §call fridge_mode on 制冷§
3.5.2.3 参数编译
参数走一个确定性编译器，逐项检查：未知参数 / 类型 / 枚举取值 / 数值上下限 / 必填项缺失 / 危险操作是否已获确认。任一不过就返回"需澄清"，不执行。
3.5.3 澄清处理
【@这里很重要的一部分 需要分清楚 什么情况下 走澄清和追问 什么情况下不走具体设计长啥样 那澄清走多少轮呢？】
3.5.4 执行下发
程序后端执行后，返回以下三种类型的回执：
结果
定义
模型动作
成功
改动生效
继续下一项任务；如结束，进行口播确认。
无需执行
部件已经是目标状态
直接告诉用户「车窗已经开着了哦」,无需重复调用
失败
工具名越界,或参数不合法
回到上一步重试
输入：§call conditioner_temperature set 99§
回执：
{"结果": "成功", 
"部件": "conditioner_temperature", 
"现态": "开启/32",
 "自动带开": ["conditioner_switch"],
 "注意": "数值 99 超范围[16,32]，已钳到 32"}

四、Context 系统
4.1 Context 组成

4.2 上下文预算与压缩
有效窗口 - 缓冲区 = 触发阈值 -> 模型进行上下文压缩
机制：生成结构化摘要 -> 替换旧消息

五、Memory管理
5.1 记忆分层
长期记忆：
层级
记忆模块
核心要素
L1 身份属性
我是谁
身份/驾照、体征、健康约束、法规角色
L2 情景偏好
我喜欢什么
舒适/娱乐/导航/交互设置、场景标签（天气/心情）
L3 驾驶指纹
我怎么开
操控/速度/决策习惯、注意力状态、异常行为基线
L4 关系网络
和谁在一起
乘员档案、社交场景（家/商/友）、优先权规则
L5 经验记忆
我经历过什么
事故/违章事件、路线记忆、反馈闭环、周期规律
写入原则：
写入必须显式，只在用户明确表达或反复确认后写入，不从单次操作推断偏好（"开过一次 18 度"不等于习惯）。
只影响默认值，不影响动作：可填参数缺省，但不能改变 Intent Head 的判定结果，也不能替用户确认危险动作。
六、权限与安全（待完成）
七、Subagents（待完成）
