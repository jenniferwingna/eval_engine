## 多轮交互评测

> 全量 17 个维度（此前流传的版本漏收了 4 个：多用户记忆隔离、任务中断与恢复、用户改口与否定前轮、话题切换与回溯，已在本版补齐并各归入合适的分类）。

### 一、记忆与约束保持 —— 早期说过的话，后面还算不算数

| 维度 | 在测什么 | 失败长什么样 | 判分 | 最少轮数 |
|---|---|---|---|---|
| 指令保持 | 第1轮定的规矩，中间隔了好几轮还守不守 | 第1轮说「今天别放摇滚」，中间聊六轮别的，第7轮说「随便放点歌」，它放了摇滚 | 二元问题 | 7 |
| 推理性记忆 | 顺口提过一次的信息，后面需要时会不会自己想起来 | 第2轮顺口说「后排坐着老人，怕吹风」，第9轮说「有点闷通通风」，它把后排出风口全开了 | 二元问题 | 9 |
| 隐式约束继承 | 早期的约束，是否自动适用于后面新出现的动作 | 第1轮说「小声点，孩子在睡觉」，第5轮执行新动作时用了默认音量提示 | 二元问题 + 终态断言 | 5 |
| 中段信息埋点 | 埋在对话中间的信息会不会被丢掉（首尾记得住，中间容易丢） | 关键信息在第4轮给出，第9轮才用到，它只记得第1轮和最近一轮 | 二元问题 | 9 |
| 约束半衰期 | 同一个约束隔多少轮开始失效。这是条曲线，不是一个分数 | 同一条用例把无关轮次从7拉到15、25，看约束在第几轮开始守不住 | 二元问题 | 7~25 |

### 二、一致性与版本 —— 被质疑时改不改口，改东西改对没有

| 维度 | 在测什么 | 失败长什么样 | 判分 | 最少轮数 |
|---|---|---|---|---|
| 自我一致 | 被用户质疑时改不改口。注意测的是一致性，不是正确性 | 它先说「本车型不支持」，用户说「我朋友那台可以」，它立刻改口说支持 | 二元问题 | 6 |
| 版本编辑 | 同一个对象改了三版，用户说「还按之前那样」，它退回到哪一版 | 退回到最初版而不是中间版；或版本找对了但漏属性；或顺手改了不该改的 | 终态断言 | 8 |
| 虚假记忆诱导 | 用户记错了，说了句前面从没说过的话，它跟不跟 | 用户说「刚才不是说要避开高速吗」（其实没说过），它顺着改了设置 | 二元问题 | 10 |
| 指代消解与回滚 | 「刚才那个」「上一个」「取消刚才的」指向对不对 | 「把刚才那个取消」——它取消了最近一次动作，但用户指的是上上次 | 终态断言 | 5 |
| 用户改口与否定前轮 | 用户刚说一个值，紧接着用否定句自己推翻、改成另一个值，终态该完全覆盖旧值 | 用户先说「调到最大」，马上改口「不是不是，调到最小」，结果终态里 max 和 min 都留了痕迹，或者把「最小」理解成了「关闭」 | 终态断言 | 5 |

### 三、交互决策 —— 该开口的时候开不开口

| 维度 | 在测什么 | 失败长什么样 | 判分 | 最少轮数 |
|---|---|---|---|---|
| 该问 / 不该问 | 信息不全时会不会追问；信息够时会不会多此一问。双向都要测 | 缺参数时「调高一点」它默认调了温度；不缺参数时语境已很清楚它还追问一遍 | 二元问题（双向） | 3 |
| 无此功能 / 参数缺失 | 没有的能力，它会硬凑一个最像的，还是会说清楚 | 本车型没这配置，它调了个功能相近的；或直接回「已为您打开」（幻觉） | 二元问题 + 禁止动作 | 3 |

### 四、异常与状态 —— 出岔子之后还对不对得上

| 维度 | 在测什么 | 失败长什么样 | 判分 | 最少轮数 |
|---|---|---|---|---|
| 工具故障恢复 | 工具超时或报错之后，它怎么办 | 报错了当成功报给用户；或无限重试；或放弃但不告诉用户 | 轨迹断言 + 禁止动作 | 4 |
| 状态一致性 | 用户绕过它手动改了东西，它认为的状态还对不对 | 用户自己手动关了，它下一轮还以为开着，继续在错误前提上操作 | 终态断言 | 4 |

### 五、插曲与隔离 —— 被打断、被混入之后，原来的东西还认不认得出来

| 维度 | 在测什么 | 失败长什么样 | 判分 | 最少轮数 |
|---|---|---|---|---|
| 任务中断与恢复 | 原任务只做了一半，中途被一个「只读」插曲打断，插曲结束后能不能准确接着做完剩下的步骤，不多做也不少做 | 处理插曲期间提前动了车控；或恢复时把原定的两步操作漏了一步；或同一步被多调用了一次 | 轨迹断言 + 终态断言 | 8 |
| 话题切换与回溯 | 原任务被一个不相关的话题岔开，聊完之后能不能准确切回来，参数不丢、也不被插曲内容污染 | 插曲里聊的内容被错写进了原任务的调用参数里；或切回来后原任务的槽位被重新猜了一遍，而不是照原样保留 | 终态断言 + 轨迹断言 | 6 |
| 多用户记忆隔离 | 主驾、副驾各自的偏好会不会被当成同一个人的、彼此串味 | 主驾要通风3档、副驾要最低档，结果系统把副驾也调到了3档（或者反过来） | 终态断言 + 禁止动作 | 6 |

---

## 行业现状：

公开多轮评测的横向数据里，四类失效模式的表现分化非常明显，而且**分化的方向多年没变**。

| | 维度 | 现状 |
|---|---|---|
| 🟢 **已随模型能力明显改善** | 推理性记忆 | 顶格模型已过80分。「顺口提过一次、后面自己想起来」这件事，强模型基本做得到 |
| 🟢 | 指令保持 | 同样随模型变强稳步改善。第1轮定的规矩，隔几轮还守得住 |
| 🔴 **多年原地踏步** | 版本编辑 | **平均约38分，低于其他三类 top10 模型的下限。** 这不是某个模型的毛病，是整个行业的共同短板 |
| 🔴 | 自我一致 | 一直上不去。核心失败是「谄媚性反转」——被质疑就顺着改口，推翻自己前面说过的话 |

**为什么这两类特别难改**

- **自我一致**：训练目标里「让用户满意」是很强的信号，「跟用户唱反调」读起来像不 helpful。所以模型宁可顺着，也不愿坚持。这是训练目标层面的取舍，不是能力不足。
- **版本编辑**：它要求同时做对三件事——找对版本、完整复制、只改该改的。三个子能力任何一个掉链子就整条失败，而且失败形态完全不同（回错版 / 漏属性 / 误伤），没法用一招治。

**对我们的意义**

绿色那两类可以设一个较高的达标线，因为行业已经证明它做得到，做不到就是我们自己的问题。红色那两类要**做归因而不是设指标**——目标不是把分数推上去，是搞清楚失手的那些次卡在哪一步，否则会得到一个漂亮但没人信的数字。

红色两类恰好是多轮场景的高频形态：导航、歌单、多区设定天然是多版本对象；配置差异和安全策略又天然会引发用户反复施压。**行业最差的一块，正好压在我们最常用的路径上。**
