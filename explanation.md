# Harness 并行链路设计说明

> 依据：`parallel` 分支（`44b58c9`），默认配置 `PARALLEL_PLANNER=omni`。
> 文中所有 prompt 尺寸、候选数量均为按仓库真实数据（547 工具 / 96 工具集 / 20 能力组）实测所得。

---

## 一、模型做工具选择时，到底看到了什么

### 1.1 先纠正一个常见误解

**这里不存在"每次工具选择发一个 prompt 出去"这回事。**

整轮对话是**同一个 Qwen3-Omni 会话、同一条 KV cache** 上的一次连续推理。所谓"三次调用"，本质是在同一条对话序列上**分三次让模型停下来说话**，每次说完不清空上下文，直接往后接。

打个比方：不是给三个人分别写三封信，而是跟同一个人连着问三个问题——他第二问时天然记得第一问答了什么，因为那些内容还在他的短期记忆（KV cache）里。

这决定了后面所有的设计取舍。

### 1.2 System prompt：只有一句，整个会话说一次

```
你是车载语音助手，请简洁回答。
```

就这么一句。**所有工具、分类、协议约束都不在 system prompt 里。** 这是刻意的：system prompt 是会话级不变量，把易变的能力清单放进去会破坏缓存复用。

### 1.3 "User query" 不是文字，是声音

这是本设计与常规 Function Calling 最大的结构差异。

用户说话时，麦克风的 PCM 被切成 1 秒一段，经音频编码器变成向量，**直接塞进模型的上下文**。整个过程中没有任何一步把语音转成文字再拼进 prompt。

**转写（ASR）是模型自己在第一次开口时顺带产出的，不是外部输入。** 客户端可能提供一份"影子转写"作为辅助提示，但协议明确要求模型输出自己确认的完整转写，以模型的为准。

### 1.4 静态前缀：塞在音频之前的"能力说明书"

在音频进入之前，系统先往上下文里放一大段固定文本，**约 25,000 字符**。内容分三块：

| 内容 | 作用 |
|---|---|
| 能力版本戳 | 标记"本轮基于哪个车型能力快照"，后面执行前要核对 |
| 第一跳输出格式规范 | 告诉模型第一次开口该说什么格式 |
| 20 个能力大类的语义说明 | 每个大类：中文标签 + 一句话描述 + 该类下所有 API 的"锚点" |

**关键：这里的 547 个工具只以「英文 ID + 一句话中文标签」的形式出现，完全没有参数、没有 schema。**

为什么要放这些锚点？因为有些功能名字很生僻（比如"星空顶""光场屏""哨兵模式"），如果只给模型 20 个大类的抽象描述，它不知道"打开星空顶"该归到哪一类。锚点让这些冷门功能变得**可路由**，但又不至于让模型在第一步就去选具体 API。

**位置很重要**：这段放在音频之前。因为它每轮都一样，模型可以复用上一轮算好的缓存，不需要重算。放在音频之后就没这个好处了。

### 1.5 第一次让模型说话：出一张"任务计划表"

用户说完话、判停触发后，系统追加一小段提示（**约 1,000 字符**），要求模型输出一行紧凑格式的计划。

提示的大意是：

> 只输出一行，不要解释。格式是：意图标签列表、完整转写、每个意图的执行模式、每个意图的前置依赖。
> 意图标签只能从这 22 个里选（20 个能力大类 + 对话 + 导航子代理）。
> 执行模式只能是：执行 / 查状态 / 闲聊 / 子代理 / 条件触发 / 被否定。
> 转写必须保留完整原话，禁止切分。
> 同一个大类里的多个操作只算一个意图，具体拆成几个调用由下一步决定。
> （语义目录已经在音频前给过了，这里不重复。）

**模型返回长这样**（以"打开车窗然后播放音乐"为例）：

```
§plan {"intents":["doors_windows","audio"],
       "transcript":"打开车窗然后播放音乐",
       "modes":["call","call"],
       "deps":[[],[1]]}§
```

一行说清了四件事：
- **转写**：我听到的原话是什么
- **意图**：涉及"门窗"和"音响"两个大类
- **模式**：两个都是要真执行的（不是提问、不是否定句）
- **依赖**：第二件事要等第一件事做完（因为原话里有"然后"）

这四件事**是一次生成出来的，在同一个 JSON 对象里**。这一点在第四部分还会展开。

格式要求极严：整段输出必须**恰好**是这一行，前后多一个字符都会被判失败。失败时不会直接报错给用户，而是把校验错误原文塞回同一条上下文，让模型重出一次（默认重试 1 次）。

### 1.6 第二次让模型说话：在小范围里选具体 API

拿到计划表后，系统才去加载 schema——**只加载被点名的那 2 个大类的**，不是全部 20 个。

然后把这些 schema 作为"工具反馈"注入上下文，要求模型选具体 API 和参数。

提示的大意是：

> 下面每个 task 是一个意图大类，它们共享同一句完整转写。
> 每个 task 只能从自己这组 schema 里选。
> 一个 task 可以输出多个调用，但不许增删或重排 task。
> 每个调用必须对应原话里明确要求的一个操作——**不许因为某个功能"可能相关"就自行展开或补充调用**。
> 如果模式是"查状态"，只选一个用于读取的 API，参数必须为空。

后面跟着按 task 分块的 schema 清单：

```
【task task-1】  转写="打开车窗然后播放音乐"  大类=doors_windows  模式=call
  1. car_door_electric_mode  控制车门的电动模式开关   参数：switch(枚举)、position(枚举)
  ...
  29. window_switch          明确打开或关闭车窗        参数：switch(枚举)、position(枚举)
  ...（共 31 项）

【task task-2】  转写="打开车窗然后播放音乐"  大类=audio  模式=call
  ...
  8. media_play              控制音乐播放              参数：action(必填, 枚举)、track(可选)
  ...（共 26 项）
```

本例总共 **约 19,500 字符**。

**规模是有硬上限的**：每个能力大类最多 40 个工具，超了在系统启动阶段就直接报错、拒绝上线。所以这段提示的长度只跟"本轮命中几个大类"有关，跟总共有 547 还是 5000 个工具**无关**。这是整个设计能扩展的关键。

**模型返回长这样**：

```
§calls {"task-1":[["window_switch",{"switch":"open"}]],
        "task-2":[["media_play",{"action":"play"}]]}§
```

同样必须严格覆盖所有 task、顺序一致、不许跨 task 选 API、不许重复调用。失败同样是把错误回灌重试。

### 1.7 第三次让模型说话：根据真实回执播报

工具执行完，把**真实回执**（哪个成功、哪个失败、失败原因）注入上下文，让模型组织一句话播报。

系统在这里附了一条强约束：不得声称失败的操作已经完成。

### 1.8 一轮的完整时间线

| 阶段 | 输入 | 模型输出 | 尺寸 |
|---|---|---|---|
| 会话建立 | 一句 system + 25K 静态前缀 | — | 一次性，可缓存 |
| 说话中 | 音频向量流 | — | — |
| 第一跳 | 1K 格式规范 | 转写 + 意图 + 模式 + 依赖（一行） | ~1K |
| 第二跳 | 命中大类的 schema | 具体 API + 参数（一行） | ~20K |
| 执行 | （不过模型） | — | — |
| 第三跳 | 真实执行回执 | 播报文本，流式切句送 TTS | 小 |

**每轮只有两次和"工具"有关的模型开口。**

---

## 二、并行执行的粒度与调度策略

### 2.1 并行的单位是什么

**一个已通过全部校验的工具调用。**

不是一句话，不是一个意图大类，是最细的那一层——单次 API 调用。

从一句话到一批可并行的调用，中间有两次展开：

1. **第一跳展开**：整句 → N 个意图任务。同一大类内的多个操作会被合并成一个意图（比如"把车窗和天窗都打开"只算一个 `doors_windows` 意图）
2. **第二跳展开**：一个意图任务 → M 个具体调用。上面那句会在这一步变成"开车窗"和"开天窗"两个独立调用，各自获得独立身份和独立资源锁

所以最终参与调度的，是展开后的调用列表。

### 2.2 调度模型：波次同步（wave-synchronous）

调度器是一个循环，每轮做三件事：

1. 从待办任务里挑出一批**可以同时跑**的
2. 把这一批全部并发启动
3. **等这一批全部结束**，再挑下一批

这是"分波推进"，不是线程池抢任务。一波内完全并发、无上限；波与波之间有屏障。

选择这个模型而不是流水线式调度的原因：车控指令有物理副作用，**批次边界就是可审计的因果边界**。每一波的成员会被记录下来，事后能回答"哪些动作是真同时发的"。

### 2.3 一批能进哪些任务：两道闸

#### 闸一：依赖就绪

任务的所有前置依赖必须全部成功。只要有一个前置失败，该任务立刻标记为"因依赖失败而失败"，不再尝试。

依赖关系来自第一跳模型给的 `deps`，并且协议强制**只能引用更早序号的任务**——所以依赖图在构造上就不可能成环。之外还会再跑一遍完整的图校验（重复 ID、自依赖、悬空边、环）。

#### 闸二：资源互斥

这是并行安全的核心。每个调用都携带一组"资源锁"。选批时按计划顺序贪心：如果一个任务需要的锁已被本批其他任务占用，它就**顺延到下一波**。

**锁的身份是怎么定的？** 分两阶段：

- **第一跳（粗粒度）**：锁就是意图大类本身，比如 `group:doors_windows`。协议强制每个执行类任务**恰好**锁自己那一个大类，多锁少锁都拒绝。对话类、导航子代理类、条件类、否定类任务**必须不锁任何资源**
- **第二跳解析后（细粒度）**：换成 `工具集 : 物理区域`。区域从已校验的参数里取（主驾/副驾/后排等），没指定就是"全车"

细化带来的实际效果：

| 用户说 | 锁 | 结果 |
|---|---|---|
| 打开主驾车窗和副驾车窗 | 同工具集，不同区域 | **同一波并行** |
| 把车窗打开再关上 | 同工具集，同区域 | **强制拆成两波** |
| 打开车窗、播放音乐 | 不同工具集 | **同一波并行** |

**锁的 ID 永远由确定性代码从静态分类树和已校验参数推导，绝不采信模型输出的自由文本。** 模型可以决定"做什么"，但不能决定"什么和什么可以同时做"。

### 2.4 失败语义：局部失败不再中断全局

一个任务失败，只有它的下游会被连带标记失败；**不相关的分支照常跑完**。

这是与旧串行实现的重要行为差异：旧实现是链式的，任一环失败整条链中断。并行实现下"打开车窗"失败不应该导致"播放音乐"也不执行——除非二者之间真的存在依赖边。

### 2.5 中断语义

用户打断（barge-in）时：所有在途任务被取消，所有"待办"和"进行中"的任务统一标记为"被中断"，并保证这个终态一定被推送到前端（不会因为取消而丢事件）。

### 2.6 事件流：回执按完成时刻流出

调度器在后台跑，回执写进一个队列，前端边跑边收。所以**用户看到的是真并发的观感**——先做完的先出结果，不用等整波结束。

尽管执行模型是波次屏障的，呈现是流式的。设计文档里可以这样区分表述：

> **执行层：资源锁约束下的 DAG 波次并发。**
> **呈现层：回执流式推送 + 生成与语音合成流水线并行。**

---

## 三、执行前的检查：有状态机，且是"全过才执行"

### 3.1 结论先行

**是的，有状态机，而且检查不止一层。任何一层不通过，都不会发出真实调用——不是执行后回滚，是根本不发。**

### 3.2 显式状态机

每个任务有四个状态：**待办 → 进行中 → 成功 / 失败**。

状态迁移规则：
- 进入"进行中"必须先过依赖闸和资源锁闸
- 回执的任务 ID 必须与任务本身匹配（防止串号）
- 失败后按重试配额决定回到"待办"还是终结为"失败"

值得注意的是：**旧串行实现里那个"全局同时只能有一个任务进行中"的互斥断言已经移除了**。互斥职责下沉到了资源锁——这是从"全局串行"到"按资源串行"的核心变化。

### 3.3 检查链（按发生顺序）

#### 第一层：协议格式

模型输出必须严格匹配约定格式，前后不能有多余文本；意图标签必须在允许集合内且不重复；转写非空；意图、模式、依赖三个数组必须等长；依赖只能指向更早的任务。

#### 第二层：安全策略反推（这一层值得单独写进设计文档）

系统用**确定性规则**从转写原文里重新判断每个子句应该是什么模式——是否定句？是条件句？是状态询问？然后**和模型自己报的模式逐项比对，不一致直接拒绝整个计划**。

也就是说：**模型对"不要开窗"的安全判断不被信任，由规则兜底否决。**

三种非执行模式的落地：
- **被否定**：记录为"已抑制"，不执行
- **条件触发**：记录为"待事件"，不执行
- **查状态**：只记录查询，并且**参数被强制清空**——防止一个"读取"意图携带了写入参数，后续被误用为控制指令

#### 第三层：授权与归属

- 选中的 API 必须属于该任务被分配的大类，跨类选择直接拒绝
- API 必须被**当前车型能力快照**放行（不同车型能力不同）
- 不许重复调用，不许增删或重排任务

#### 第四层：参数编译

参数走一个确定性编译器，逐项检查：未知参数 / 类型 / 枚举取值 / 数值上下限 / 必填项缺失 / 危险操作是否已获确认。任一不过就返回"需澄清"，不执行。

#### 第五层：执行前的最后一道闸

真正发出调用之前，还要再核对一次**能力快照版本戳**。

场景：如果在本轮规划进行中，后台装载了一份新的车型能力清单，那么基于旧清单做出的计划就是过期的。系统会判定计划失效并拒绝执行，而不是静默地按旧假设执行下去。

这本质上是**乐观并发控制**：规划时记下版本，执行前校验版本没变。

### 3.4 检查链全貌

```
模型第一跳输出
   ↓ 格式严格匹配？
   ↓ 意图在闭集内？模式与意图类型匹配？
   ↓ 规则反推的安全模式 == 模型自报模式？
   ↓ 资源锁纪律正确？
   ↓ 依赖图无环、无悬空边？
模型第二跳输出
   ↓ 覆盖所有任务、顺序一致？
   ↓ API 属于本任务大类？被能力快照放行？
   ↓ 参数通过 schema 编译？危险操作已确认？
   ↓ 展开后依赖图再校验一次
调度
   ↓ 依赖就绪？
   ↓ 资源锁不冲突？
   ↓ 能力快照版本仍然有效？
──────────────────────────
才 发 出 真 实 调 用
```

---

## 四、关于"五个 head 并行"——需要修正的前提

### 4.1 事实澄清

**在当前实现里，这五个东西不是一个模型并行输出的五个神经网络头。**

它们的真实形态：

| 你说的 head | 实际是什么 | 在哪 |
|---|---|---|
| ASR head | 第一跳输出里的 `transcript` 字段 | 同一次生成 |
| Intent head | 第一跳输出里的 `intents` 字段 | **同一次生成** |
| （依赖图） | 第一跳输出里的 `modes` / `deps` 字段 | **同一次生成** |
| Tool head | 第二跳的输出 | 同一条上下文的后续生成 |
| Duplex head | 判停 sidecar | **独立进程** |
| Audio | 语音合成服务 | **独立进程** |

也就是说：前四个是**同一条自回归流上的顺序角色**，后两个是**独立服务**。没有任何两个是"并行的头"。

### 4.2 那会不会冲突？分四种情况，答案不同

#### (a) 转写 / 意图 / 模式 / 依赖 之间 —— 结构上不可能冲突

它们是**一个 JSON 对象的四个字段，一次生成出来的**，并且做联合一致性校验：三个数组等长、模式与意图类型必须匹配、资源锁必须与意图标签严格对应、依赖必须单向。

这不是"生成完再对齐"，是"要么整体通过，要么整体作废"。

设计文档里可以称之为 **原子首跳绑定（atomic first-hop binding）**。

不需要状态管理。

#### (b) 第一跳与第二跳之间 —— 靠会话状态机保证，已实现

Tool Head 天然看得见第一跳的输出，因为它们在同一条上下文里，物理上排在后面。播报天然看得见执行回执，同理。

**不存在需要协调的并发**——它们是严格串行的。

模型会话本身有一个小状态机保证顺序不被违反：
- 轮次未开始就注入观察 → 报错
- 上一次注入的观察还没被消费就再注入 → 报错
- 轮次已开始却又想传入新的上下文（而不是走观察通道）→ 报错

这三条断言把"先后顺序"变成了运行时不可违反的约束。

#### (c) 判停组件与主链路 —— 需要陈旧性管理，已实现，且策略变了

**这里有一个重要的设计变更需要写进文档：判停的权威来源已经从语义判停切换到能量判停（700ms 静音）。语义判停 sidecar 被降级为"影子"，它的判定结果会被明确标记为"未采纳"。**

影子组件被移到独立的队列和工作协程上，与主音频摄入路径解耦（避免它的网络延迟拖慢音频链路）。它的每条结果都带轮次标签，**如果轮次已经切换、或主链路正在处理，结果整批丢弃**。

这是典型的陈旧结果防护。

#### (d) 能力版本与在途计划 —— 需要乐观并发控制，已实现

见 3.3 第五层。

### 4.3 所以，我需要做状态管理吗？

**需要，但不是为"五头冲突"做，而是为这三件事做：**

| 要管的状态 | 解决什么问题 | 现状 |
|---|---|---|
| 模型轮次状态机 | 保证"规划 → 选工具 → 播报"顺序不被违反 | 已有，三条运行时断言 |
| 轮次 ID 陈旧性 | 上一轮的异步结果不能污染这一轮 | 已有，带轮次标签 + 整批丢弃 |
| 能力快照版本 | 车型能力变更时，在途计划必须失效而非误执行 | 已有，版本戳 + 执行前校验 |

**一句话总结给设计文档：**

> 并行性存在于**执行层**（资源锁约束下的 DAG 波次并发），不存在于模型输出层。模型侧是单流多跳，一致性靠上下文顺序和联合校验保证，而非靠状态协调。需要显式状态管理的只有三处：模型轮次状态机、轮次陈旧性、能力快照版本。

---

## 附：设计文档可直接引用的几条表述

1. **非检索式工具选择**：候选集是静态分类树的确定性子集，运行时不做向量检索、不做 BM25、不做 top-k 召回。
2. **两跳规划**：第一跳只做"听清 + 归类 + 定序"，不接触任何 API schema；第二跳只在被点名的少数大类内选具体 API。
3. **规模无关性**：提示词长度只与本轮命中的大类数量相关，与工具总量无关。每组工具数有硬上限，超限在启动期即拒绝上线。
4. **模型建议、规则裁决**：模型输出的意图、模式、参数全部是建议值；安全模式由规则反推否决，参数由确定性编译器校验，资源锁由代码从分类树推导。模型不能绕过任何一道闸。
5. **并行边界即因果边界**：批次由依赖就绪性与资源互斥共同决定，批次成员被记录，事后可审计。
