## 1. 功能描述

一个**全双工、流式、零检索**的座舱 Agent Harness：

- **单模型双头**：MoLA-Omni 同时承担对话生成与意图判定，不做多 Agent 编排。用户说话的同时模型就在听、在判停、在出意图。
- **零检索**：车控 API 清单在训练阶段进入模型，Intent Head 一次直出唯一 API。不存在向量库、BM25、工具卡片召回，也不存在 top-k 候选或"给模型几个让它挑"。
- **全链路流式**：音频流式进、意图流式出、工具流式调、语音流式播；任意阶段可被用户打断。

## 2. 核心模块设计

### 2.1 总体架构

> 《总体架构图》配图待补

### 2.2 语音侧：感知与生成

关注点：**延迟与可打断**——用户说完到出第一个字要快，说话时要能随时截停。

#### 2.2.1 全双工前端

| 项 | 设计 |
| --- | --- |
| 音频粒度 | 上行 16 kHz PCM16LE，按 **160 ms** 定长切块，不足一块不发 |
| 判停依据 | **语义**而非静默时长：判停器逐块返回 `listen` / `speak`，短停顿、语气词、拖长音不误判为说完 |
| 增量回显 | 每块状态里文本一变就发 `transcript.partial`，用户看到自己"边说边出字" |
| 判停触发 | 转 `speak` 即闭合本轮：发 `transcript.final` + `turn.end`，启动处理 |
| 打断门控 | 处理进行中，只有**确实含语音**的帧才算插话，环境噪声不算；能力由开关控制，关闭时处理期间一律不打断 |
| 打断动作 | 停掉生成与工具执行，发 `turn.interrupted`，再用新 `turn_id` 开一轮；客户端凭 `turn_id` 丢弃旧轮残帧 |
| 故障降级 | 判停器挂掉发 `recoverable` 错误事件，不断连；语音判停失效但手动 / 文本提交仍可用 |

#### 2.2.2 MoLA-Omni 流式生成

| 机制 | 设计 |
| --- | --- |
| KV 常驻 | 一个会话一份持久 KV，system 段与 `<\|audio_start\|>` 在会话建立时就写进去 |
| 音频入 KV | 说话期间音频按 **1 秒窗口**编码后追加进同一份 KV，不满一秒的尾巴在闭合时补齐 |
| 闭合本轮 | 追加 `<\|audio_end\|>` + **确定性上下文** + assistant 起始标记，然后解码 |
| 零 token 提交 | rules 已把话说完时仍走一次 `max_new_tokens=0` 的闭合，**只闭合 KV 不生成**，保证下一轮音频上下文连续 |
| 增量出字 | 一个汉字常由多个 token 拼成，只解出前半截会得到占位符 `�`；**文本不含 `�` 才吐增量**，下游断句与 TTS 拿不到半个汉字 |
| 打断 | 逐 token 循环每转一圈检查取消标志，一立即停在当前 token 返回 |
| 隔离 | 工具调用生成走**另一份一次性 KV**，共享权重但不碰会话缓存，中间 token 不污染对话历史 |

### 2.3 决策侧：意图与路由

#### 2.3.1 单意图链路

![image](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026082112406841/49549684/54f7095e587347529909d7ff7608ad66.png)

**两个头的分工**

| 头 | 输出 | 作用 |
| --- | --- | --- |
| Intent Head | 走哪条分支（dialog / tool / subagent），tool 分支下直出唯一车控 API 与置信度 | 决定**去哪** |
| ASR Head | 本轮用户到底说了什么（规整后的 query 文本） | 决定**带什么去** |

**三条分支**

| 分支 | 处理方式 | 出口 |
| --- | --- | --- |
| dialog | 不加载任何 Schema，直接由 MoLA-Omni 续写 | agent response（流式） |
| tool | 直出唯一 API → 装载该 API 的 Schema → 生成调用 → 执行 → 拿回执 | rules 或 MoLA-Omni 播报 |
| subagent | 交给子代理完成一段多步任务，产出步骤计划与播报摘要 | subagent response |

#### 2.3.2 车控 API 检查

| 核对项 | 说明 | 例 |
| --- | --- | --- |
| API id 落在运行时已注册的清单内 | 硬错误，不做最近邻纠偏——模型返回"车门"不会被改成最像的"车窗" | 车门不在已注册的 546 个 API 里 |
| 置信度在 `[0,1]` 内 | 硬错误——模型给出的置信度必须是合法数值 | Intent Head 返回 confidence=1.5 |
| 置信度 ≥ 该 API 声明的阈值 | **转澄清**——停下来反问用户一句，不硬猜。每个 API 可声明不同的置信度要求，"开后备箱"可能要 ≥0.8，"关车窗"可能要 ≥0.5 | Intent Head 给 0.6，但该 API 声明要 ≥0.8 |



**版本风险：** 车控 API 清单进了模型权重。新增或下线一个 API 就需要重新训练。

### 2.4 执行侧：调度与回执

关注点：**任务状态始终可见、回执始终可信**。

多意图有**两套调度模式**，共用同一个参数编译器、同一种回执协议，差别只在"任务从哪来"和"能不能同时跑"。

| 维度 | 串行 | 并行 |
| --- | --- | --- |
| 任务来源 | 确定性 **query 切割**（按规则分割后各段独立过 Intent Head） | 一次***结构化规划**|
| 并发度 | 同时只跑一个任务 | 同一个**就绪批次**里的任务同时跑 |
| 顺序保证 | 链式依赖：按原句顺序一个接一个 | **依赖图**(即 DAG，有向无环图） |
| 冲突避免 | 天然无冲突 | **资源锁**：锁的是**物理部件**，不是 API。"升主驾车窗"和"降主驾车窗"是两个 API 但同一把锁，不能同时跑 |
| Schema 槽位 | 全局单槽，同时只装载一份 Schema | 每个并发任务各持有自己那份 Schema，用完各自释放 |
| 适合场景 | 强顺序依赖、状态敏感 | 意图彼此独立、延迟敏感 |


*结构化规划 :一个独立的规划模块（规划器）负责把**整句原文**规划成多个有序的任务，包含各任务的 API、依赖关系、原文依据等。与之相对，**query 切割**是用确定性规则把一句话按标点和连接词分割成多句

#### 2.4.1 工具执行链

**一、前置约束：Schema 与版本保证**
**版本校验**——Schema 索引的条目数必须与已注册 API 清单一致，对不上拒绝启动；

**二、参数编译：六步顺序校验**

| 顺序 | 检查什么 | 例 | 不通过 |
| --- | --- | --- | --- |
| 1 | 分类置信度是否达到该 API 声明的阈值 | 开后备箱要求 ≥0.8，Intent Head 只给 0.6 | `low_confidence` |
| 2 | Schema 本身是否可用 | Schema 文件损坏、`properties` 不是对象 | `invalid_schema` |
| 3 | 参数名是否都是 Schema 声明过的 | 开天窗只认 `position`，模型多给了一个 `speed` | `unknown_parameters` |
| 4 | 逐参数过四关：缺失则从 query 推断 → 别名归一 → 类型强转 → 枚举 / 取值域校验 | 空调 Schema 取值域 16–30，用户说"调到 40 度" | `invalid_parameter:<名>` |
| 5 | `required` 是否全部就位 | 说了"调温度"却没说调到几度 | `missing_required:<名>` |
| 6 | `x-dangerous` 的 API 是否拿到用户明确确认词 | 行驶中要开后备箱，用户没说"确认" | 要求确认 |

任一不通过项一律**转澄清**（停下来等用户答一句明确的确认或参数）。


**三、执行与回执**

回执统一为 `{success, payload, error}`，是判断执行是否成功的**唯一**依据。失败就如实播报，禁止用模板伪造。

**四、播报分流**

只有**本轮全部任务都是 API 调用且全部成功**时才走 rules 模板直答，省掉一次模型往返；走 rules 时仍对主模型做一次零 token 闭合以保持 KV 连续。
涉及对话澄清、子代理、任何失败或空结果，一律回到 MoLA-Omni 基于真实回执组织回答。

#### 2.4.2 串行：Query 切割 + 单任务锁

切割策略：**保守且确定性**：

| 规则 | 说明 |
| --- | --- |
| 显式序列边界 | 标点（`。！；`）与序列连接词（然后 / 接着 / 之后 / 再 / 同时 / 并且 / 还有 / 另外 / 顺便 等）直接切 |
| 名词并列（和 / 跟 / 与） | **仅当**两侧各自独立指向部件族且两族不相交时才切。"打开仪表屏和车窗"切；"主驾和副驾的车窗"不切 |
| 动词继承 | 只有部件没有动作的短片段（"关闭车窗还有天窗" → "天窗"）继承前一片段动词；限 4 字以内纯部件片段，带谓语的子句不动 |

**注：subagent 分支整句不切。**

#### 2.4.3 并行：意图切分 + 并发直出

规划器：**把整句切成若干意图，每个意图各过一次 Intent Head 直出 API**。

**四项校验**，任一不过整份计划判非法，不做局部修补：

| 校验项 | 说明 | 不过例 |
| --- | --- | --- |
| 清单内 | 每个任务的 API id 必须在已注册清单内 | 规划器返回"车门"但已注册 API 里没这个 → 报错，不能硬改成"车窗" |
| 原文依据 | 每个任务必须携带一段**逐字取自原句**的 `evidence`。一个任务如果在原话里找不到对应的字，它就不该存在 | 用户说"打开车窗和音乐"，规划器硬造出第三个任务"调大音量"但原句里没这几个字 → 报错 |
| 无环 | id 唯一、不得自依赖、不得引用未知 id、图不得成环（DAG 特性） | 任务 A 依赖 B，B 依赖 C，C 又依赖 A → 成环 → 报错 |
| 资源锁 | 任务未显式声明时，按该 API 的部件标识派生默认锁名。同一部件的多个操作得分配同一把锁，保证不并发 | "升主驾车窗"和"降主驾车窗"是两个不同 API，但都涉及"主驾车窗"这个部件 → 派生同一把锁 → 不能同时跑 |

规划失败不抛异常——返回一份带澄清标记的空计划，由调度层转成如实的失败回执，不断连接。

**调度流程**——任务进 Task Agenda （规划器）后，调度层按以下规则进行：

| 场景 | 动作 |
| --- | --- |
| **就绪批次** | 依赖都已完成 + 占用的部件锁互不冲突的任务，组成一个批次同时跑；一批全部结束再算下一批 |
| **前序失败** | 前序任务失败，后继任务直接判 `dependency_failed`，不进批 |
| **图阻塞** | 还有待办任务却凑不出就绪批次，判定依赖图被阻塞，直接报错、不空转 |
| **被打断** | 打断信号来时，取消整个正在跑的批次，所有未完成任务标 `interrupted`，状态如实广播 |

并行模式**没有 rules 播报通道**：批次结束后统一组装上下文交给模型播报。rules 直答目前只存在于串行模式。

#### 2.4.4 澄清回合

置信度不足、必填缺失、危险动作未确认——三类都不是错误，而是需要向用户要一句话。

| 场景 | Harness 播报什么 | 用户怎么回答 | 接下来 |
| --- | --- | --- | --- |
| **置信度不足** | "我没太听清，能再说一遍吗？" | 重复一遍意图，比如"打开天窗" | 用新的转写重新过参数编译 |
| **必填参数缺失** | "你没说要调到几度啊" | 补充参数，比如"22 度" | 回填该参数、续跑原任务 |
| **危险动作未确认** | "行驶中打开后备箱比较危险，你确认吗？" | 明确确认，比如"我确认" | 拿到确认词、续跑原任务 |

**流程细节**：

1. 任务在参数编译或分类时被判需要澄清，转入 `Waiting` 态，保留已编译的部分参数（不重头来）
2. Harness 播报一个**由拒绝原因码生成**的问题（不是模型自由发挥）
3. 等待用户下一轮输入：
   - **是在回答澄清问题**（内容包含上面表格右列的内容）→ 回填参数、续跑该任务
   - **是新的意图**（内容是别的，比如"关车窗"）→ 丢弃挂起任务、按新意图重新开始
4. 挂起有超时（如 30 秒无回应）与轮次上限（如连续答非所问 3 次），超限时如实告知用户、该任务判失败

例子：用户说"打开天窗"，模型觉得不够确定，Harness 问"我没太听清，能再说一遍吗？" → 用户说"打开天窗"或"打开顶窗" → Harness 带新转写重新分类 → 置信度上来了 → 继续跑。

### 2.5 贯穿态：上下文与记忆

#### 2.5.1 Context 组成

![image](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026082112406841/49465490/6e4146de1e384b62bb2b13a4b447aeff.png)

#### 2.5.2 上下文预算与压缩

有效窗口 -  缓冲区 = 触发阈值 -> 模型进行上下文压缩
机制：生成结构化摘要 -> 替换旧消息 

#### 2.5.3 记忆分层

| 层 | 存什么 | 生命周期 | 落点 | 状态 |
| --- | --- | --- | --- | --- |
| 工作记忆 | 本轮任务列表、回执、当前 Schema | 一轮，轮结束即清 | `TASK_PLAN` / `EXTERNAL_FEEDBACK` / `CURRENT_TASK` | 进行中 |
| 情景记忆 | 本次行程内发生过什么、已建立的指代（"刚才那个"、"再调高一点"） | 一次连接 / 一段行程 | `HISTORY`，超窗走摘要 | 待定 |
| 长期记忆 | 用户稳定偏好（惯用温度、座椅位置、常用目的地）、设备与车辆画像 | 跨行程持久 | 独立存储，按需注入 `SYSTEM` 尾部 | 待定 |

写入原则：

- **长期记忆是"注入"不是"检索"**：按用户 id 直接取一份定长偏好档案，不做相似度召回——与整体零检索取向一致。
- **写入必须显式**，只在用户明确表达或反复确认后写入，不从单次操作推断偏好（"开过一次 18 度"不等于习惯）。
- **只影响默认值，不影响动作**：可填参数缺省，但不能改变 Intent Head 的判定结果，也不能替用户确认危险动作。

### 2.6 对外接口：协议与数据结构

| 对接方 | 方向 | 传输 | Harness 收到什么 | Harness 给出什么 | 例 |
| --- | --- | --- | --- | --- | --- |
| 车机客户端 | 双向 | WebSocket | 音频块（16 kHz PCM16LE）、文本提交、打断信号 | 事件帧序列 | 客户端送音频 → Harness 发 `transcript.partial` → 发 `tool.receipt` → 发 `tts.audio.delta` |
| 车控执行器 | Harness 发起 | HTTP POST | 回执 `{success, payload, error}` | 请求 `{task_id, tool_id, arguments}` | Harness 发 `{task_id: "T1", tool_id: "open_window", arguments: {window: "main", position: 50}}` → 执行器返回 `{success: true, payload: {...}}` |
| Intent Head 服务 | Harness 发起 | HTTP POST | API id + 置信度 | 本轮 query 文本 | 客户端说"打开天窗" → Harness 发 query → Intent Head 返回 `{api_id: "open_sunroof", confidence: 0.92}` |
| MoLA-Omni 引擎 | Harness 发起 | WebSocket | 文本增量 | 音频块、闭合指令、确定性上下文 | Harness 发闭合指令 + 确定性上下文 → Qwen 引擎逐 token 流回文本增量 |
| API Schema 资产 | 静态读取 | 文件系统 | —— | 每 API 一份 JSON Schema 文件 + 一份索引 | `/schemas/open_window.json`、`/schemas/index.json` |

#### 2.6.1 事件帧

下行统一信封 `{type, payload, sequence, timestamp_ms, turn_id}`。`sequence` 会话内单调递增，客户端据此判乱序与丢帧；`turn_id` 每次打断后换新，客户端据此丢弃上一轮残帧。

| 组 | 事件 |
| --- | --- |
| 会话与轮次 | `session.ready`（会话 id、音频格式与采样率）／`turn.started`／`turn.end`／`turn.interrupted` |
| 听 | `transcript.partial`／`transcript.final`；判停器 `listen`/`speak` 状态帧 |
| 想 | `classification.path`（本轮判定的 API 与置信度）／`agenda.classification`（多意图下每任务一条）／`agenda.state`（全量任务列表）／`tool.receipt`／`subagent.response` |
| 说与观测 | `llm.delta`／`llm.done`；`response.rule`（rules 直答，增量带 `source: rules`）／`tts.audio.delta`（PCM16LE 24 kHz）／`tts.done`／`latency`（`first_token`/`first_audio`，每条带 `from` 标明锚点）／`error`（带 `stage` 与 `recoverable`） |

#### 2.6.2 数据结构

**已注册 API 清单**：当前规模 **546 个车控 API**。

**API Schema**（每 API 一份独立文件）：`tool_id`（与索引互校）、`properties`（参数名 → 类型 / 枚举 / 取值域）、`required`、`x-dangerous`（需显式确认）、`x-min-confidence`（该 API 要求的最低置信度）、`x-aliases`（参数别名）。

**任务对象**：两种模式共用 `id / query / status / attempts / depends_on / tool_id / result / error` 及一条纠正痕迹列表。并行模式额外携带规划信息 `evidence / support_span / arguments / resources / confidence`。

**工具回执**：`{task_id, success, payload, error}` —— 判断执行是否成功的**唯一**依据。

## 3. 开发模块拆解

| 组 | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **A 主链路** | A1 API Schema 标注补齐 | 为全量 Schema 补齐 `required` / `x-dangerous` / `x-min-confidence` / `x-aliases` | 目录源数据 | P0 | 待启动 |
| **A 主链路** | A2 Intent Head | 车控 API 训练进模型、导出、侧车推理接入、直出唯一 API | 标注数据、清单版本 | P0 | 进行中 |
| **A 主链路** | A2b ASR Head | | | | 待定 |
| **A 主链路** | A3 API 清单与版本握手 | 清单构建流水线；启动时模型清单版本 ⇄ Schema 索引版本比对，不一致拒绝启动 | A1 / A2 | P0 | 待启动 |
| **A 主链路** | A4 全双工前端 | 判停、打断门控、延迟锚点埋点 | —— | P0 | 进行中 |
| **A 主链路** | A5 单意图主链路 | 三分支分流 + 单份 Schema 装载 + 参数编译 + 执行 + 播报分流 | A2 / A3 | P0 | 进行中 |
| **A 主链路** | A6 流式 TTS | 增量断句 + 流式音频 + 打断即停 | A4 | P1 | 进行中 |
| **A 主链路** | A6b 确定性纠正栈 | 锚点纠正与可审计痕迹；作用域边界需重新定义后再评估是否保留 | A5 | P2 | 待重新定义 |
| **A 主链路** | A7 多意图·串行调度 | 切割、澄清回合、单任务锁、依赖传播、重试 | A5 | P1 | 进行中 |
| **A 主链路** | A8 多意图·并行调度 | 意图切分 + 并发直出、四条计划校验、依赖图、资源锁、批次执行、分槽装载 | A5 | P1 | 进行中 |
| **A 主链路** | A9 Subagent 通道 | 子代理任务协议、结果回填、与工具分支的边界 | A5 | P1 | 待细化 |
| **B 记忆与上下文** | B1 记忆体系与上下文 | 工作记忆（进行中）、情景记忆与压缩策略（待定）、长期记忆注入（待定）、会话持久化（待定） | A5 | P2 | 混合 |
| **C 交互与安全** | C1 澄清回合与确认交互 | 澄清回合的问题生成与追问流程、危险动作确认 | A5 | P1 | 待定 |
| **C 交互与安全** | C2 车辆状态与上下文 | 车辆状态接入、说话人与座位归属、响应侧校验 | A5 | P2 | 待定 |
| **D 工程支撑** | D1 前端事件协议 | 事件帧定义、序号与乱序处理、客户端联调 | A4 / A5 | P1 | 进行中 |
| **D 工程支撑** | D2 可观测性与韧性 | 可观测性、外部服务韧性、弱网与离线降级 | A5 | P2 | 待定 |
| **D 工程支撑** | D3 沙箱与评测 | 沙箱环境（暂时还没有规划）、评测框架 | A5 | P3 | 待定 |
