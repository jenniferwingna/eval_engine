# 全双工零检索座舱 Agent Harness 设计方案

## 1. 功能描述

一个**全双工、流式、零检索**的座舱 Agent Harness。用户说话的同时模型就在听、在判停、在出意图；意图确定后不做任何召回，直接沿一棵静态分类树逐层收敛到唯一工具，只加载这一个工具的 Schema，生成调用、拿到真实回执后再流式播报。

三条核心取向：

- **单模型双头**：MoLA-Omni 同时承担对话生成与意图判定，不做多 Agent 编排。
- **零检索**：候选集永远来自分类树的确定性子节点，不存在向量库、BM25、工具卡片召回或 top-k 工具选择。
- **全链路流式**：音频流式进、意图流式出、工具流式调、语音流式播；任意阶段可被用户打断。

## 2. 核心模块设计

模块按**对外承诺什么**分为五层，而非按调用顺序。

### 2.1 总体架构

> 《总体架构图》配图待补

音频与生成是**两条并行的流**：前端在判停时，主模型已经在把音频吃进 KV。判停一到，主模型无需重新编码历史音频，直接闭合本轮即可出字——首字延迟的主要来源之一在这里被消掉。

### 2.2 语音侧：感知与生成

> 承诺：**延迟与可打断**。用户说完到出第一个字要快，说话时要能随时截停。

#### 2.2.1 全双工前端

| 项 | 设计 |
| --- | --- |
| 音频粒度 | 上行 16 kHz PCM16LE，按 **160 ms** 定长切块，不足一块不发 |
| 判停依据 | **语义**而非静默时长：判停器逐块返回 `listen` / `speak`，短停顿、语气词、拖长音不误判为说完 |
| 增量回显 | 每块状态里文本一变就发 `transcript.partial`，用户看到自己"边说边出字" |
| 判停触发 | 转 `speak` 即闭合本轮：发 `transcript.final` + `turn.end`，启动处理 |
| 打断门控 | 处理进行中，只有**确实含语音**的帧才算插话，环境噪声不算；能力由开关控制，关闭时处理期间一律不打断 |
| 打断动作 | 停掉生成与工具执行，发 `turn.interrupted`，再用新 `turn_id` 开一轮；客户端凭 `turn_id` 丢弃旧轮残帧 |
| 故障降级 | 判停器挂掉发 `recoverable` 错误事件，不断连；语音判停失效但手动 / 文本提交仍可用 |

**门控为什么必须有**：麦克风是一条不会停的流，若"来帧就打断"，每一轮都会在刚开始处理时被随后涌入的帧取消掉，等于永远说不出话。

**延迟锚点统一**：所有里程碑以**判停时刻**为零点，只埋 `first_token` 与 `first_audio` 两个点。锚点统一是后续一切优化能横向比较的前提。

#### 2.2.2 MoLA-Omni 流式生成

| 机制 | 设计 |
| --- | --- |
| KV 常驻 | 一个会话一份持久 KV，system 段与 `<\|audio_start\|>` 在会话建立时就写进去 |
| 音频入 KV | 说话期间音频按 **1 秒窗口**编码后追加进同一份 KV，不满一秒的尾巴在闭合时补齐 |
| 闭合本轮 | 追加 `<\|audio_end\|>` + **确定性上下文** + assistant 起始标记，然后解码 |
| 零 token 提交 | rules 已把话说完时仍走一次 `max_new_tokens=0` 的闭合，**只闭合 KV 不生成**，保证下一轮音频上下文连续 |
| 增量出字 | 一个汉字常由多个 token 拼成，只解出前半截会得到占位符 `�`；**文本不含 `�` 才吐增量**，下游断句与 TTS 拿不到半个汉字 |
| 打断 | 逐 token 循环每转一圈检查取消标志，一立即停在当前 token 返回 |
| 隔离 | 工具调用生成走**另一份一次性 KV**，共享权重但不碰会话缓存，中间 token 不污染对话历史 |

### 2.3 决策侧：意图与路由

> 承诺：**只在给定选项里选，选不出就问**。每层可选项由分类树直接给出；模型答了选项之外的东西即报错，不替它"找个最像的"圆场；答得出但没把握即**转澄清**——停下来反问用户一句，不硬猜。全文"转澄清"均指此。

#### 2.3.1 单意图链路

> 《单意图链路图》配图待补 —— 图需表达：user query → MoLA-Omni → Intent Head 定去向、ASR Head 定入参 → 唯一 Schema → 参数编译 → 执行 → 回执。

**两个头的分工**

| 头 | 输出 | 作用 |
| --- | --- | --- |
| Intent Head | 走哪条分支（dialog / tool / subagent），及逐层分类决策与置信度 | 决定**去哪** |
| ASR Head | 本轮用户到底说了什么（规整后的 query 文本） | 决定**带什么去** |

**三条分支**

| 分支 | 处理方式 | 出口 |
| --- | --- | --- |
| dialog | 不加载任何 Schema，直接由 MoLA-Omni 续写 | agent response（流式） |
| tool | 逐层收敛到唯一工具 → 装载 Schema → 生成调用 → 执行 → 拿回执 | rules 或 MoLA-Omni 播报 |
| subagent | 交给子代理完成一段多步任务，产出步骤计划与播报摘要 | subagent response |

**subagent 的硬约束**：子代理不直接下发车控动作，只产出计划与摘要；真要动车仍回到 tool 分支走 Schema + 参数编译 + 回执。多步任务不能成为绕过参数校验的后门。

#### 2.3.2 逐层受限分类

> 《逐层受限分类图》配图待补 —— 图需表达：tool intent 逐层下钻 大类 → 工具集 → 工具 → 唯一 Schema；每层两条旁路：候选集逃逸 → 硬错误，置信度不足 → 转澄清。

候选集由上一层的选中 id 在树上**确定性推导**，不做相似度、不排序、不 top-k。顶层固定三选一；空工具集的大类在构造候选集时剔除，模型不会看到"选了也走不下去"的选项。

**Harness 不信任分类器的输出**。分类器是个模型，可能返回树上不存在的 id、对不上的层级、越界的置信度，所以它的返回不是结论而是待核对的答卷。例：候选集是「车窗 / 天窗 / 后备箱」，模型返回"车门"——不纠正成最像的"车窗"，直接判错，**猜错的代价远高于问一句**。

| 核对项 | 不通过 |
| --- | --- |
| 合法 JSON、层级对得上 | 硬错误 |
| 每层 id 落在该层候选集内 | 硬错误，不做最近邻纠偏 |
| 置信度在 `[0,1]` 内 | 硬错误 |
| 置信度达到阈值 | 转澄清 |

分类树带**完整性块**，启动即校验、对不上拒绝启动，避免模型输出空间与实际目录悄悄错版。当前校验四项：声明工具数 = 实际条目数（树与索引各一次）、三层 id 各自唯一、父子引用存在、标记 canonical 时必须恰好 546 个工具且 Schema 全为规范版。

### 2.4 执行侧：调度与回执

> 承诺：**任务状态始终可见、回执始终可信**。模型不得声称一件没做成的事做成了。

多意图有**两套调度模式**，共用同一棵分类树、同一份 Schema 装载器、同一个参数编译器、同一种回执契约，差别只在"任务从哪来"和"能不能同时跑"。

| 维度 | 串行 | 并行 |
| --- | --- | --- |
| 任务从哪来 | 确定性 **query 切割** | 一次**结构化规划**，整句不预切 |
| 并发度 | Doing 唯一 | 就绪批次内并发 |
| 顺序保证 | 链式依赖 | DAG `depends_on` |
| 冲突避免 | 天然无冲突 | **资源锁**（同一部件同一把锁） |
| Schema 槽位 | 全局单槽 | 按任务 id 分槽 |
| 适合场景 | 强顺序依赖、状态敏感 | 意图彼此独立、延迟敏感 |

#### 2.4.1 Agent Loop 与工具执行链

> 《Agent Loop 图》配图待补 —— 图需表达：组装六段 Context → 生成 → 是否调用工具 → 执行 → 回执写回 EXTERNAL_FEEDBACK + TASK_PLAN → 回到组装；不调用则流式播报结束。

环内「执行工具」不是一次调用，而是一条有闸门的链。

**第一步：Schema 单份装载。** 按工具 id 取文件加载，**任何时刻只持有一份**，任务结束立即释放。

> "只持有一份"是这套方案的立身之本：上下文里只有一份 Schema，模型就没有"在几个工具之间挑"的机会。一旦允许放多份，无论 3 份还是 20 份，做的事就等价于"召回若干个让模型挑"，零检索的前提当场失效。附带好处是归因干净——参数出错只可能是这一份的事。

装载时顺带**校验**（索引条目数与目录声明一致，对不上拒绝启动）与**防串**（路径必须落在 schemas 目录内，`tool_id` 必须与请求一致）。

**第二步：参数编译。** 唯一的合法性关口，模型输出无法绕过，按固定顺序过闸：

| 顺序 | 检查 | 不通过 |
| --- | --- | --- |
| 1 | 分类置信度 ≥ 该工具要求的最低置信度 | `low_confidence` |
| 2 | Schema 的 `properties` 结构有效 | `invalid_schema` |
| 3 | 参数名必须都是 Schema 声明过的（显式允许附加属性时除外） | `unknown_parameters` |
| 4 | 逐参数：缺失则从 query 推断 → 别名归一 → 类型强转 → 枚举 / 取值域校验 | `invalid_parameter:<名>` |
| 5 | `required` 全部就位 | `missing_required:<名>` |
| 6 | `x-dangerous` 工具必须有用户明确确认词 | 要求确认 |

全部不通过项一律**转澄清**，不降级执行。顺序本身是契约——先查置信度还是先查必填，决定用户听到哪一句追问。

第 4 步的**语义值到 Schema 拼写适配**是关键：用户说"打开"，而不同工具的枚举可能写成 `on` / `open` / `1`。编译器先把口语归一到语义值，再在该 Schema 的枚举里找能落的拼写，落不上判非法，不硬塞。

两处开关：**入口过滤**——模型给的参数先按 Schema 过一遍，没声明的键当场扔掉（主链路已如此，上表第 3 步是给其他调用方的兜底）；**推断开关**——第 4 步的"从 query 推断"可关闭，关闭后只校验不补参，用来单独衡量模型自己填得准不准。

**第二步补充：确定性纠正栈（可选，默认关闭）。** 分类与编译之间还有一层任务局部纠正，修正同一工具集内的常见混淆，三条硬边界：**不越工具集**（只在已选定工具集内换，换不出去）、**不猜**（靠工具名在原话里的字面锚点，锚点不唯一即弃权）、**全程留痕**（每级产出 `{stage, before, after, changed, reason}`，关闭时也产出 `reason=disabled`，保证开关两种运行可对齐比较）。并行模式多一级「候选补全」：规划器给了候选工具却没定下具体工具时，从候选集内确定性补一个。

**第三步：执行与回执。** 回执统一为 `{success, payload, error}`，是判断成功与否的**唯一**依据：没有回执 = 失败，不存在"模型认为成功"；失败就如实播报，禁止用模板伪造成"已为您完成"；执行完立即释放 Schema 槽位。

**第四步：播报分流。** 只有**本轮全部任务都是工具调用且全部成功**时才走 rules 模板直答，省掉一次模型往返；沾上对话、子代理、任何失败或空结果，一律回到 MoLA-Omni 基于真实回执组织回答。走 rules 时仍对主模型做一次零 token 闭合以保持 KV 连续。

回执写回 `EXTERNAL_FEEDBACK` 与 `TASK_PLAN` 两段——模型对"哪些做完了、哪些失败了"的认知来自这两段，不靠自己记。

#### 2.4.2 串行：Query 切割 + 单任务锁

> 《串行调度图》配图待补 —— 图需表达：user query → 切割 → Task Agenda → 取单任务锁执行（Doing 唯一）→ 全部完成？未完成回 Agenda 取下一个。

切割必须**保守且确定性**——宁可少切，不可错切：

| 规则 | 说明 |
| --- | --- |
| 显式序列边界 | 标点（`。！；`）与序列连接词（然后 / 接着 / 之后 / 再 / 同时 / 并且 / 还有 / 另外 / 顺便 等）直接切 |
| 名词并列（和 / 跟 / 与） | **仅当**两侧各自独立指向部件族且两族不相交时才切。"打开仪表屏和车窗"切；"主驾和副驾的车窗"不切 |
| 动词继承 | 只有部件没有动作的短片段（"关闭车窗还有天窗" → "天窗"）继承前一片段动词；限 4 字以内纯部件片段，带谓语的子句不动 |

"是不是部件族"用分类树自带的部件锚点判断，不用外部词典——切割与分类共用同一份目录事实。

**例外：subagent 分支整句不切。**"先去买点菜，然后回家"里的"然后"是行程单，不是句子边界；整句作为一个任务交给子代理，连接词由子代理在计划里展开。

#### 2.4.3 并行：一次规划 + DAG + 资源锁

并行**不靠切句**——整句原样交给规划器，规划结果必须对应原始整句，改写过的输入直接判非法。规划有两种形态，配置切换，差别在于**模型每次调用要面对多少选项**：

| 形态 | 怎么规划 | 提示词里放什么 |
| --- | --- | --- |
| **分层规划**（默认） | 第一步小候选调用做意图抽取 + 大类判定；第二步各意图「工具集 → 工具」下钻**并发进行** | 每步只放当前层的小候选集，**全程不放全目录** |
| 整份规划 | 一次调用产出全部任务的完整字段 | 放**扁平化的全量目录**（约三万 token），靠恒定前缀命中前缀缓存摊薄成本，故指令与目录在前、query 在后 |

以"把空调调到 22 度，然后打开天窗"为例：分层规划先问"几个意图、各属哪个大类"得到「空调」「车身」，再两路并发各自下钻，每次只面对几个到几十个选项；整份规划一次把 546 个工具全摆出来，模型直接吐出两个任务连同参数与先后关系——调用次数最少，但每次提示词都很大。分层规划第二步是真并发，墙钟时间取决于最慢的一路，不随意图数线性增长。

> 《并行规划与批次执行图》配图待补 —— 图需表达：整句 → 意图抽取 + 大类判定 → 各意图下钻并发 → 计划校验 → 任务 DAG + 资源锁 → 按就绪批次并发执行。

**两种形态的保证强度不同**，这是选型时最需要知道的一点：

| 保证 | 分层规划（默认） | 整份规划 |
| --- | --- | --- |
| 候选集不逃逸 | 每层当场对候选集校验，逃逸即判错 | 事后校验三层父子关系与树一致 |
| 原文依据 | 要求逐字取自原句；定位不到仅记为无位置，**不阻断** | **强校验**：必须是原句连续子串，模型给的 offset 只作参考，定位不到即整份判非法 |
| 依赖图 | 只从"然后 / 再 / 接着"生成依赖，并列意图相互独立 | **完整图校验**：id 唯一、不得自依赖、不得引用未知 id、**不得有环** |
| 资源锁 | **不产出**，并发冲突需靠依赖表达 | 产出：未声明时按部件标识或所属工具集派生默认锁名 |

`evidence` 逐字来自原句是并行防幻觉的关键：一个任务如果在原话里找不到对应的字，它就不该存在。这条在整份规划里是硬校验，分层规划目前只定位不阻断，需补齐（见 §3 A8）。规划失败不抛异常——返回带澄清标记的空计划，由调度层转成如实的失败回执，不断连接。

**调度**

| 机制 | 说明 |
| --- | --- |
| 就绪批次 | 依赖全部 `Done` 且资源锁互不冲突的任务组成一批 |
| 批内并发 / 批间串行 | 一批同时跑，全批结束再算下一批 |
| 依赖失败传播 | 前序失败，后继直接判 `dependency_failed`，不进批 |
| 图阻塞 | 有待办却凑不出就绪批次，直接判定阻塞并报错，不空转 |
| 打断 | 取消整批，未完成任务标 `interrupted`，状态如实广播 |

并行模式**没有 rules 播报通道**：批次结束后统一组装上下文交给模型播报。rules 直答目前只存在于串行模式。

#### 2.4.4 任务状态机与澄清回合

> 《任务状态机图》配图待补 —— 图需表达：Todo → Doing（串行取得单任务锁 / 并行进入就绪批次）→ Done（回执 success）/ Failed（回执 failure、重试耗尽、被打断、前序依赖失败）/ Waiting（需澄清；回答回填转回 Doing，超时或改主意转 Failed）。

| 不变量 | 说明 |
| --- | --- |
| 并发受控 | 串行下 Doing 唯一；并行下 Doing 集合内资源锁两两不冲突 |
| 挂起不占位 | 转 `Waiting` 时让出执行位，后续任务可先跑，一句没问清不卡死整轮 |
| 重试有上限 | 按配置次数重试，耗尽即终态 |
| 全量可见 | 每次状态变化广播**全量任务列表**并写回上下文，前端不自己维护状态 |

**澄清回合**：置信度不足、必填缺失、危险动作未确认——三类都不是错误，而是需要向用户要一句话。任务转 `Waiting` 并播报一个**由拒绝原因码生成**的问题（问什么由编译器的原因码决定，不让模型自由发挥），下一轮先判断用户这句是不是在回答它：是就回填参数续跑（保留已编译的部分参数，不重头再来），不是就丢弃挂起任务、按新意图重新开始。挂起有超时与轮次上限，作废时如实告知。

### 2.5 贯穿态：上下文与记忆

> 承诺：**模型看到的每一段都有确定的写入方**。上下文不是拼出来的自由文本，而是六段各有归属、各有更新节奏的结构。

#### 2.5.1 Context 组装

| 段 | 内容 | 更新节奏 |
| --- | --- | --- |
| `SYSTEM` | 角色与硬约束（含"不得声称失败操作已完成"） | 会话级，恒定 |
| `HISTORY` | 对话历史 | 会话级 |
| `CURRENT_INPUT` | 本轮用户原始整句（非切分后的子任务） | 轮级 |
| `TASK_PLAN` | 当前 Agenda 全量任务及状态 | 每次组装**整体覆盖** |
| `EXTERNAL_FEEDBACK` | 已产生的全部工具回执 | **只增不减** |
| `CURRENT_TASK` | 当前任务 + **唯一一份**工具 Schema | 任务级，用完即释放 |

三条约束：`CURRENT_TASK` 任何时刻最多一个工具的 Schema，不存在"塞一堆让模型挑"；工具 id 与 Schema 必须成对出现，只给其一即报错；结构化段落以紧凑 JSON 序列化、段间用标签包裹，保证同样状态每次拼出的 prompt 完全一致（可复现、可缓存）。

两种模式组装节奏不同：**串行**每完成一个任务组装一次，`CURRENT_TASK` 指向刚跑完的任务；**并行**在整批结束后统一组装一次，`CURRENT_TASK` 为空——批内没有"当前唯一任务"的概念。

**上下文预算与压缩**（待定）。会膨胀的是 `HISTORY`（随会话）、`TASK_PLAN`（随任务数）、`EXTERNAL_FEEDBACK`（随回执体积）三段。压缩策略在设计上定死三条原则：裁剪顺序固定为「老历史 → 回执详情 → 任务中间过程」，`SYSTEM` 硬约束与 `CURRENT_TASK` 的 Schema 永不裁剪；任何截断都在原位留标记，不让模型看到"看似完整实则缺失"的上下文；摘要只陈述已发生的动作与回执结论，不推断、不把失败写成成功。

#### 2.5.2 记忆分层

按**生命周期**与**可信度**分三层，不混在一起：

| 层 | 存什么 | 生命周期 | 落点 |
| --- | --- | --- | --- |
| 工作记忆 | 本轮任务列表、回执、当前 Schema | 一轮，轮结束即清 | `TASK_PLAN` / `EXTERNAL_FEEDBACK` / `CURRENT_TASK` |
| 会话记忆 | 本次行程内的对话历史与已建立的指代（"刚才那个"、"再调高一点"） | 一次会话 / 一段行程 | `HISTORY`，超窗走摘要 |
| 长期记忆 | 用户稳定偏好（惯用温度、座椅位置、常用目的地）、设备与车辆画像 | 跨会话持久 | 独立存储，按需注入 `SYSTEM` 尾部 |

- **长期记忆是"注入"不是"检索"**：按用户 id 直接取一份定长偏好档案，不做相似度召回——与整体零检索取向一致。
- **写入必须显式**，只在用户明确表达或反复确认后写入，不从单次操作推断偏好（"开过一次 18 度"不等于习惯）。
- **只影响默认值，不影响动作**：可填参数缺省，但不能改变分类结果，也不能替用户确认危险动作。
- **可查可删**：用户能问"你记住了我什么"，也能要求清除。

### 2.6 对外接口：协议与数据契约

> 承诺：**接口稳定**。事件帧与数据结构是下游可以依赖、不会被内部重构悄悄改掉的部分。

#### 2.6.1 事件帧

下行统一信封 `{type, payload, sequence, timestamp_ms, turn_id}`。`sequence` 会话内单调递增，客户端据此判乱序与丢帧；`turn_id` 每次打断后换新，客户端据此丢弃上一轮残帧。

| 组 | 事件 |
| --- | --- |
| 会话与轮次 | `session.ready`（会话 id、音频格式与采样率）／ `turn.started`／`turn.end`／`turn.interrupted` |
| 听 | `transcript.partial`／`transcript.final`；判停器 `listen`/`speak` 状态帧 |
| 想 | `classification.level`（逐层一条，带候选数与置信度）／`classification.path`／`agenda.classification`（多意图下每任务一条）／`agenda.state`（全量任务列表）／`tool.receipt`／`subagent.response` |
| 说与观测 | `llm.delta`／`llm.done`；`response.rule`（rules 直答，增量带 `source: rules`）／`tts.audio.delta`（PCM16LE 24 kHz）／`tts.done`／`latency`（`first_token`/`first_audio`，每条带 `from` 标明锚点）／`error`（带 `stage` 与 `recoverable`） |

逐层发 `classification.level` 而不只发最终结果，是为了让"错在哪一层"当场可见——上层选错，下层再准也回不来。

#### 2.6.2 数据契约

**分类树**：三层结构，节点带 `id / label / description / 父节点 id`，顶层带完整性块。当前规模 **546 工具 / 96 工具集 / 10 大类**，其中 1 个大类无工具集、构造候选集时剔除，实际可选 **9 个**。长尾明显：平均每工具集 5.7 个工具，最大 **73** 个，另有 **32** 个只含单工具——末层候选集长度极不均衡，是判准的主要风险来源。

**工具 Schema**（每工具一份独立文件）：`tool_id`（与索引互校）、`properties`（参数名 → 类型 / 枚举 / 取值域）、`required`、`x-dangerous`（需显式确认）、`x-min-confidence`（该工具要求的最低分类置信度）、`x-aliases`（参数别名）。

**任务对象**：两种模式共用 `id / query / status / attempts / depends_on / tool_id / result / error` 及一条纠正痕迹列表。并行模式额外携带规划信息 `category / toolset / class_path / evidence / support_span / candidate_tools / arguments / resources / confidence`。

**工具回执**：`{task_id, success, payload, error}` —— 判断执行是否成功的**唯一**依据。

## 3. 开发模块拆解

未启动的模块留空并标注状态，不预先编造交付内容。

**A · 主链路**

| # | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A1 | 工具目录标注补齐 | 为全量 Schema 补齐 `required` / `x-dangerous` / `x-min-confidence` / `x-aliases` | 目录源数据 | P0 | 待启动 |
| A2 | Intent Head | 训练、导出、侧车推理接入 | 标注数据、目录版本 | P0 | 进行中 |
| A2b | ASR Head | | | | 待定 |
| A3 | 分类树构建与校验 | 目录 → 三层分类树流水线 + 完整性校验；运行时源文件哈希比对 | A1 | P0 | 进行中 |
| A4 | 全双工前端 | 判停、打断门控、延迟锚点埋点 | —— | P0 | 进行中 |
| A5 | 单意图主链路 | 三分支分流 + 单份 Schema 装载 + 参数编译 + 执行 + 播报分流 | A2 / A3 | P0 | 进行中 |
| A6 | 流式 TTS | 增量断句 + 流式音频 + 打断即停 | A4 | P1 | 进行中 |
| A6b | 确定性纠正栈 | 同工具集内锚点纠正与候选补全、可审计痕迹；默认关闭，需评估后决定是否默认开启 | A5 | P2 | 进行中 |
| A7 | 多意图·串行调度 | 切割、状态机、单任务锁、依赖传播、重试 | A5 | P1 | 进行中 |
| A8 | 多意图·并行调度 | 双形态规划、计划校验、DAG、资源锁、批次执行、分槽装载；分层规划的原文依据强校验与资源锁产出待补齐 | A5 | P1 | 进行中 |
| A9 | Subagent 通道 | 子代理任务协议、结果回填、与工具分支的边界 | A5 | P1 | 待细化 |

**B / C / D · 尚未展开的模块**

交付内容与依赖待各模块启动时填写，此处只锁定归属与状态。

| 组 | 模块 | 状态 |
| --- | --- | --- |
| B 记忆与上下文 | B1 会话记忆 | 待定 |
| B 记忆与上下文 | B2 上下文预算与压缩 | 待定 |
| B 记忆与上下文 | B3 长期记忆 | 待定 |
| B 记忆与上下文 | B4 会话持久化 | 待定 |
| C 交互与安全 | C1 澄清回合 | 待定 |
| C 交互与安全 | C2 危险动作确认交互 | 待定 |
| C 交互与安全 | C3 车辆状态接入 | 待定 |
| C 交互与安全 | C4 说话人与座位归属 | 待定 |
| C 交互与安全 | C5 响应侧校验 | 待定 |
| D 工程支撑 | D1 前端事件协议（事件帧定义、序号与乱序处理、客户端联调；依赖 A4 / A5，P1） | 进行中 |
| D 工程支撑 | D2 可观测性 | 待定 |
| D 工程支撑 | D3 外部服务韧性 | 待定 |
| D 工程支撑 | D4 弱网与离线降级 | 待定 |
| D 工程支撑 | D5 沙箱 | 暂时还没有规划这个 |
| D 工程支撑 | D6 评测 | 待定 |
