# 全双工零检索座舱 Agent Harness 设计方案

| 项 | 内容 |
| --- | --- |
| 版本 / 状态 | v0.3 / 草稿 |
| 负责人(文字) / 配图 / 评审 | / / |
| 适用范围 | 车载座舱语音助手的 Agent 运行时（Harness） |

> 本文只描述三件事：**要做成什么样（功能描述）**、**怎么搭（核心模块设计）**、**怎么排开发（开发模块拆解）**。

---

## 1. 功能描述

### 1.1 目标形态

一个**全双工、流式、零检索**的座舱 Agent Harness。用户说话的同时模型就在听、在判停、在出意图；意图确定后不做任何召回，直接沿一棵静态分类树逐层收敛到唯一工具，只加载这一个工具的 Schema，生成调用、拿到真实回执后再流式播报。

三条设计主线：

- **单模型双头**：MoLA-Omni 同时承担对话生成与意图判定，不做多 Agent 编排。
- **零检索**：候选集永远来自分类树的确定性子节点，不存在向量库、BM25、工具卡片召回或 top-k 工具选择。
- **全链路流式**：音频流式进、意图流式出、工具流式调、语音流式播；任意阶段可被用户打断。

### 1.2 明确不做

- 不做向量检索、BM25、工具卡片召回、RAG 上下文注入。
- 不做 top-k 工具候选再排序；工具定位是分类而非检索。
- 不做多 Agent 互相调度；只有一个主模型 + 受控的 subagent 通道。
- 不做"模型说成功就算成功"；没有回执就是失败。

---

## 2. 核心模块设计

### 2.1 总体架构

> 《总体架构图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：客户端 → 全双工前端（语义判停·打断门控）→ MoLA-Omni 双头（Intent Head / ASR Head）→ 意图路由（大类→工具集→工具）→ 参数编译 → 工具执行 → 回执写回 Context → 流式播报。

音频与生成是**两条并行的流**：前端在判停，主模型同时已经在把音频吃进 KV。判停一到，主模型不需要重新编码历史音频，直接闭合本轮即可出字——这是首字延迟的主要来源之一被消掉的地方。

### 2.2 全双工前端：判停与打断

| 项 | 设计 |
| --- | --- |
| 音频粒度 | 上行 16 kHz PCM16LE，按 **160 ms（2560 采样）** 定长切块；不足一块不发 |
| 判停信号 | 判停器逐块返回状态：`listen`（还在说）/ `speak`（说完了），并带增量识别缓冲 |
| 判停依据 | **语义**而非静默时长——短停顿、语气词、拖长音不会误判为说完 |
| 增量回显 | 每块状态里的文本变化就发一次 `transcript.partial`，用户看到自己在"边说边出字" |
| 判停触发 | 状态转 `speak` 即闭合本轮：发 `transcript.final` + `turn.end`，启动处理任务 |
| 打断门控 | 由开关控制。开启时，处理进行中收到的音频块**只有确实含语音**才触发打断；关闭时处理期间的语音一律忽略，不打断在跑的轮次 |
| 打断动作 | 取消模型生成与本轮处理任务，发 `turn.interrupted`；随后换新 `turn_id` 并重发 `turn.started` |
| 判停器故障 | 降级而非断连：发一条 `recoverable` 错误事件，语音判停失效但手动/文本提交仍可用 |

打断门控这条是必须的：麦克风是连续流，处理期间一定持续有帧进来，若"来帧即打断"，每一轮都会在刚开始处理时被自己取消掉。

**延迟锚点**：所有延迟里程碑统一以 **判停时刻** 为零点，只埋两个点——`first_token`（第一个可播的字）和 `first_audio`（第一个音频块）。锚点统一是后续一切优化能横向比较的前提。

### 2.3 MoLA-Omni 流式生成

| 机制 | 设计 |
| --- | --- |
| KV 常驻 | 一个会话一份持久 KV，system 段与 `<\|audio_start\|>` 在会话建立时就写进去 |
| 音频入 KV | 用户说话期间，音频按 **1 秒窗口**编码后直接追加进同一份 KV；不满一秒的尾巴在闭合时补一次（短于 200 ms 丢弃） |
| 闭合本轮 | 追加 `<\|audio_end\|>` + **确定性上下文** + assistant 起始标记，然后开始解码 |
| 增量出字 | 逐 token 贪心解码，只在解码结果不含替换字符时才吐增量——**不吐半个汉字**，下游断句和 TTS 不会拿到乱码 |
| 零 token 提交 | rules 已经把话说完时，仍然走一次 `max_new_tokens=0` 的闭合，**只闭合 KV 不生成**，保证下一轮音频上下文连续 |
| 打断 | 生成循环每步检查取消标志，打断后立即停在当前 token |
| 工具调用生成 | 走**另一份一次性 KV**，与流式会话共享权重但完全不碰它的缓存，避免把工具调用的中间 token 污染进对话历史 |

### 2.4 单意图链路

> 《单意图链路图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：user query → MoLA-Omni → Intent Head 定去向、ASR Head 定入参 → 唯一 Schema → 参数编译 → 执行 → 回执。

**两个头的分工**

| 头 | 输出 | 作用 |
| --- | --- | --- |
| Intent Head | 走哪条道（dialog / tool / subagent），以及逐层的分类决策与置信度 | 决定**去哪** |
| ASR Head | 本轮用户到底说了什么（规整后的 query 文本） | 决定**带什么去** |

两头输出在路由处汇合：Intent Head 定下 tool 道，ASR Head 的 tool query 就是该工具 Schema 下的生成输入；Intent Head 定下 subagent 道，ASR Head 的 subagent query 就是子代理的任务描述。

**三条道分别做什么**

| 道 | 处理方式 | 出口 |
| --- | --- | --- |
| dialog（树上的 id 为 `conversation`） | 不加载任何 Schema，直接由 MoLA-Omni 续写 | agent response（流式） |
| tool | 逐层收敛到唯一工具 → 装载 Schema → 生成调用 → 执行 → 拿回执 | rules 或 MoLA-Omni 播报 |
| subagent | 交给子代理独立完成一段多步任务，产出步骤计划与播报摘要 | subagent response |

subagent 道有一条硬约束：**子代理不直接下发车控动作**，它产出的是计划与摘要；真要动车，仍然回到 tool 道走 Schema + 参数编译 + 回执那一套。这样"多步任务"不会成为绕过参数校验的后门。

### 2.5 意图路由：逐层受限分类

从 tool intent 到具体 Schema 的这一步是**分类**，不是检索：

> 《意图路由逐层受限分类图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：tool intent 逐层下钻 大类 → 工具集 → 工具 → 唯一 Schema；每层两条旁路：候选集逃逸→硬错误（不猜不纠偏）、置信度不足→转澄清。

**候选集怎么来**：由上一层的选中 id 在分类树上**确定性推导**出子节点集合，不做任何相似度计算、不做排序、不做 top-k。顶层固定三选一（dialog / tool / subagent）；空工具集的大类在候选集构造时就被剔除，模型不会看到一个选了也走不下去的选项。

**两种落地形态，同一套候选集**

| 形态 | 用途 | 特点 |
| --- | --- | --- |
| Intent Head | 主用。独立进程 / 侧车，短超时（秒级）调用，一次返回全路径决策 | 快、可单独训练与迭代，不占主模型解码 |
| 受限提示分类 | 冷启动、对照、Head 不可用时的兜底 | 逐层把候选集喂给模型，要求返回严格 JSON `{id, confidence}` |

**Harness 不信任分类器的输出**。无论哪种形态，返回的每一层都要在 Harness 侧对着候选集重新校验一遍：

| 校验 | 不通过的处理 |
| --- | --- |
| 返回是否是合法 JSON、层级是否合法 | 硬错误 |
| 每层 id 是否在该层候选集内 | 硬错误（不做最近邻纠偏，不做"猜一个像的"） |
| 置信度是否在 `[0,1]` | 硬错误 |
| 置信度是否达到阈值 | 转澄清，不硬猜 |

分类树带**完整性块**，启动即校验，对不上直接拒绝启动——避免模型的输出空间和实际目录悄悄错版。当前落地的校验项：

| 校验项 | 说明 |
| --- | --- |
| 声明工具数 = 实际条目数 | 分类树与 Schema 索引各校验一次 |
| id 唯一 | 工具 / 工具集 / 大类三层分别去重 |
| 父子引用存在 | 工具的所属工具集、工具集的所属大类必须都在树里 |
| 规范目录约束 | 标记为 canonical 时，必须恰好 546 个工具且 Schema 全部为规范版 |

完整性块里还记录了**目录源文件的 SHA-256**，目前由构建流水线在生成时写入，用于版本追溯；运行时的哈希比对尚未接入（见 §3 A3）。

### 2.6 工具执行链

**第一步：Schema 单份装载**

按工具 id 从索引取文件名并加载，任何时刻只持有一份，任务结束立即释放。三道防护：

- 索引条目数必须与完整性块声明的工具总数一致，否则拒绝启动；
- 解析出的路径其父目录必须就是 schemas 根目录，防索引被篡改后的路径穿越；
- 加载到的 Schema 里的 `tool_id` 必须与请求的 id 一致，防张冠李戴。

**第二步：参数编译**——唯一的合法性关口，模型输出不能绕过。按固定顺序过闸：

| 顺序 | 检查 | 不通过 |
| --- | --- | --- |
| 1 | 分类置信度 ≥ 该工具要求的最低置信度 | 转澄清 `low_confidence` |
| 2 | Schema 的 `properties` 结构有效 | 转澄清 `invalid_schema` |
| 3 | 模型给的参数名不得超出 `properties`（除非显式允许附加属性） | 转澄清 `unknown_parameters` |
| 4 | 逐参数：缺失则从 query 推断 → 别名归一 → 按声明类型强转 → 枚举 / 最小最大值校验 | 转澄清 `invalid_parameter:<名>` |
| 5 | `required` 全部就位 | 转澄清 `missing_required:<名>` |
| 6 | `x-dangerous` 工具必须有用户明确确认词 | 转澄清，要求确认 |

第 4 步里的**语义值到 Schema 拼写的适配**是关键：用户说"打开"、模型给出语义动作，而不同工具的枚举可能写成 `on` / `open` / `1`。编译器先把口语归一到语义值，再在该 Schema 的枚举里找能落的拼写，落不上就判非法，而不是硬塞。

模型生成的实参在进入编译器之前，先按 Schema 的 `properties` 过滤掉一切未声明的键——模型只能填这份 Schema 认识的参数。编译器的"从 query 推断"能力同样受开关控制：关闭时编译器只做校验、不做推断，参数完全来自模型输出。

**第二步补充：确定性纠正栈（可选，默认关闭）**

在分类结果与参数编译之间还有一层**任务局部的确定性纠正**，用来修正同一工具集内部的常见混淆。它有三条硬边界：

- **不越工具集**：纠正只在已分类工具集的候选集内进行，传入的工具若不在该工具集内直接报错，纠正不可能把结果带到别的分支去；
- **不猜**：靠工具标签 / 执行器名在任务原文里的字面锚点判断，锚点不唯一就弃权保持原结果，不做模糊匹配；
- **全程留痕**：每一级纠正都产出一条 `{stage, before, after, changed, reason}` 记录挂在任务上，随任务状态一起广播，事后可逐条复盘。

整个纠正栈受环境开关控制、**默认关闭**，关闭时同样产出 `reason=disabled` 的痕迹，保证有无纠正的运行都能对齐比较。并行模式下还多一级「候选补全」：规划器给出了候选工具却没定下具体工具时，从候选集内确定性地补一个。

**第三步：执行与回执**

执行器把编译后的实参发给外部工具，回执统一为 `{success, payload, error}`。这是判断执行是否成功的**唯一**依据：

- 没有回执 = 失败，不存在"模型认为成功"这种状态；
- 失败就是失败，向用户如实播报，禁止用模板伪造成"已为您完成"；
- 执行完立即释放 Schema 槽位。

**第四步：播报分流（rules 还是 MoLA-Omni）**

只有当**本轮全部任务都是工具调用、且全部拿到成功回执**时，才允许走 rules 模板直接播报，省掉一次模型往返，首字延迟最低。只要沾上对话、子代理、任何失败或空结果，一律回到 MoLA-Omni，让它基于真实回执组织回答。走 rules 时仍然对主模型做一次零 token 闭合，保证 KV 连续。

### 2.7 Context 组装

生成侧的上下文是**固定六段**，顺序固定、边界带标签，模型不需要猜哪段是什么：

| 段 | 内容 | 生命周期 |
| --- | --- | --- |
| `SYSTEM` | 角色与硬约束（含"不得声称失败操作已完成"这类禁令） | 会话级 |
| `HISTORY` | 对话历史 | 会话级 |
| `CURRENT_INPUT` | 本轮用户原始输入 | 轮级 |
| `TASK_PLAN` | 当前 Agenda 全量任务及状态 | 轮级 |
| `EXTERNAL_FEEDBACK` | 已产生的全部工具回执 | 轮级 |
| `CURRENT_TASK` | 当前任务 + **唯一一份**工具 Schema | 任务级，用完即释放 |

三条约束：

1. `CURRENT_TASK` 里任何时刻最多一个工具的 Schema。不存在"把所有工具塞进上下文让模型挑"，也不存在"检索若干个塞进去"。
2. 工具 id 与 Schema 必须成对出现，只给其一即报错——避免出现"有 Schema 不知道是谁的"或"点了名却没给约束"。
3. 结构化段落以紧凑 JSON 序列化，段间用标签包裹，保证同样的状态每次拼出的 prompt 完全一致（可复现、可缓存）。

两种调度模式的组装节奏不同：**串行**每完成一个任务组装一次，`CURRENT_TASK` 指向刚跑完的那个任务；**并行**在整批任务全部结束后统一组装一次，`CURRENT_TASK` 为空——批内没有"当前唯一任务"这个概念，任务信息全部落在 `TASK_PLAN` 与 `EXTERNAL_FEEDBACK` 里。

**上下文预算与压缩**

六段里 `SYSTEM` 与 `CURRENT_TASK` 长度可控，会膨胀的是另外三段：`HISTORY` 随会话变长、`TASK_PLAN` 随任务数变长、`EXTERNAL_FEEDBACK` 随回执体积变长。座舱单轮虽短，但一旦引入跨轮记忆就必然要有预算机制，因此压缩策略在设计上就定死，不留到出事再补：

| 段 | 预算策略 | 压缩手段 |
| --- | --- | --- |
| `SYSTEM` | 定长，硬上限 | 不压缩 |
| `HISTORY` | 滚动窗口 + 超窗摘要 | 保留最近 N 轮原文，更早的折叠成一段结构化摘要（做过什么、当前车内状态、未决事项） |
| `TASK_PLAN` | 全量保留任务行，但只保留摘要字段 | `id / query / status / error`，不带中间过程 |
| `EXTERNAL_FEEDBACK` | 单条回执 payload 有大小上限 | 超限时保留 `success`、错误码与关键字段，长文本截断并标记 `truncated` |
| `CURRENT_TASK` | 只有一份 Schema，天然有界 | 不压缩 |

三条原则：

1. **先裁最不影响正确性的**。裁剪顺序固定为：老历史 → 回执详情 → 任务中间过程；`SYSTEM` 的硬约束与 `CURRENT_TASK` 的 Schema 永不裁剪。
2. **压缩必须留痕**。任何被截断的内容都在原位留标记，模型知道"这里有过东西但被裁了"，而不是看到一段看似完整实则缺失的上下文。
3. **摘要不改事实**。历史摘要只允许陈述已发生的动作与回执结论，不允许推断、不允许把失败写成成功。

### 2.8 记忆分层

Harness 的记忆分三层，边界按**生命周期**和**可信度**划分，不混在一起：

| 层 | 存什么 | 生命周期 | 落点 |
| --- | --- | --- | --- |
| 工作记忆 | 本轮的任务列表、回执、当前 Schema | 一轮，轮结束即清 | `TASK_PLAN` / `EXTERNAL_FEEDBACK` / `CURRENT_TASK` |
| 会话记忆 | 本次行程内的对话历史与已建立的指代（"刚才那个"、"再调高一点"） | 一次会话 / 一段行程 | `HISTORY`，超窗走摘要 |
| 长期记忆 | 用户稳定偏好（惯用温度、座椅位置、常用目的地、称呼方式）、设备与车辆画像 | 跨会话持久 | 独立存储，按需注入 `SYSTEM` 尾部 |

关键设计：

- **长期记忆是"注入"不是"检索"**。它按用户 id 直接取一份定长的偏好档案注入，不做相似度召回——这条和整体零检索的取向一致。
- **写入必须显式**。只有用户明确表达偏好、或某个设置被反复确认后才写长期记忆，不从单次操作里自动推断偏好，避免"开过一次 18 度"就被记成习惯。
- **长期记忆只影响默认值，不影响动作**。它可以填参数缺省（没说温度就用惯用温度），但不能改变意图分类结果，也不能替用户确认危险动作。
- **可查可删**。用户能问"你记住了我什么"，也能要求清除；这是合规底线，不是加分项。

### 2.9 澄清回合

分类置信度不足、必填参数缺失、危险动作未确认——这三类都不是错误，而是**需要向用户要一句话**。澄清必须是一个完整的回合，而不是把任务判失败了事：

> 《澄清回合流程图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：任务执行 → 是否需澄清（low_confidence / missing_required / dangerous_action）→ 任务挂起 Doing→Waiting → 生成澄清问题并播报 → 等待用户回答 → 是该问题的回答则回填续跑，否则丢弃挂起任务按新意图重来。

| 设计点 | 说明 |
| --- | --- |
| 挂起而非失败 | 任务进入等待态并保留已编译出的部分参数，用户答完直接续跑，不重头再来 |
| 问题由拒绝原因生成 | 参数编译器给出的原因码（缺哪个参数、哪个工具危险、哪层置信度不够）直接决定问什么，不让模型自由发挥 |
| 有超时和轮次上限 | 用户不接话或连续答非所问，挂起任务超时作废并如实告知，不无限等待 |
| 用户改主意优先 | 下一轮若是新意图而非回答，直接放弃挂起任务，不强迫用户先答完 |


### 2.10 Agent Loop

标准 Agent Loop，无特殊设计：

> 《Agent Loop 图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：组装六段式 Context → 模型生成 → 是否调用工具 → 执行工具 → 回执写回 EXTERNAL_FEEDBACK + TASK_PLAN → 回到组装；不调用则流式播报结束。

回执写回的落点是 `EXTERNAL_FEEDBACK` 与 `TASK_PLAN` 两段，下一轮生成看到的是被更新过的上下文——模型对"哪些做完了、哪些失败了"的认知来自这两段，不靠自己记。

### 2.11 多意图调度：串行与并行

多意图有**两套调度模式**，共用同一棵分类树、同一份 Schema 装载器、同一个参数编译器、同一种回执契约；差别只在"任务从哪来"和"能不能同时跑"。

| 维度 | 串行模式 | 并行模式 |
| --- | --- | --- |
| 任务从哪来 | 确定性 **query 切割** | 一次**结构化规划**，整句不预切 |
| 并发度 | Doing 唯一 | 就绪批次内并发 |
| 顺序保证 | 链式依赖 | DAG `depends_on` |
| 冲突避免 | 天然无冲突 | **资源锁**（同一硬件部件同一把锁） |
| Schema 槽位 | 全局单槽 | 按任务 id 分槽 |
| 适合场景 | 强顺序依赖、状态敏感、要求可预期 | 意图彼此独立、延迟敏感 |

#### 2.11.1 串行：Query 切割 + 单任务锁

> 《串行调度图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：user query → MoLA-Omni → query 切割 → Task Agenda → 取单任务锁执行 task_i（Doing 唯一）→ 是否全部完成 → 未完成回 Agenda 取下一个。

**Query 切割**把一句包含多个意图的话切成多个独立任务。切割必须是**保守且确定性**的——宁可少切，不可错切：

| 规则 | 说明 |
| --- | --- |
| 显式序列边界 | 标点（`。！；`）与序列连接词（然后 / 接着 / 之后 / 再 / 随后 / 同时 / 并且 / 以及 / 还有 / 另外 / 顺便）直接切 |
| 名词并列（和 / 跟 / 与） | **只有**两侧各自独立指向部件族、且两族不相交时才切。"打开仪表屏和车窗"切；"主驾和副驾的车窗"不切 |
| 动词继承 | 切出的短片段若只有部件没有动作（"关闭车窗还有天窗" → "天窗"），继承前一片段的动词。只有 4 字以内的纯部件片段才继承，本身带谓语的子句不动 |

判断"是不是部件族"用的是分类树自带的部件锚点，不是外部词典——切割和分类共用同一份目录事实。

**一条重要例外：subagent 道整句不切。**"先去买点菜，然后回家"里的"然后"是这趟行程的行程单，不是两句话的边界。判到 subagent 道时整句作为一个任务交给子代理，序列连接词由子代理自己在计划里展开。

#### 2.11.2 并行：一次规划 + DAG + 资源锁

并行**不靠切句**——整句原样交给规划器，规划结果必须对应原始整句，改写过的输入直接判非法。规划有两种形态，通过配置切换：

| 形态 | 怎么规划 | 提示词里放什么 |
| --- | --- | --- |
| **分层规划**（默认） | 第一步一次小候选调用做意图抽取 + 大类判定；第二步各意图的「工具集 → 工具」下钻**并发进行** | 每步只放当前层的小候选集，**全程不放全目录** |
| 整份规划 | 一次调用直接产出全部任务的完整字段 | 放**扁平化的全量工具目录**（约三万 token），靠恒定前缀命中服务端前缀缓存来摊薄成本；因此指令与目录在前、可变 query 在后 |

分层规划的第二步是真并发，整体墙钟时间取决于最慢的那一路，而不随意图数线性增长：

> 《并行分层规划与批次执行图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：整句 → 第一步意图抽取+大类判定（一次小候选调用）→ 第二步各意图「工具集→工具」下钻并发进行 → 计划校验 → 任务 DAG + 资源锁 → 按就绪批次并发执行。

**规划出的任务携带**：

| 字段 | 用途 |
| --- | --- |
| `category / toolset / tool` | 三层分类结果；判到对话或子代理道时后两层为空 |
| `class_path` | 三层路径，自校验用 |
| `evidence` + `support_span` | 该任务在原句中的**逐字原文依据**及其位置 |
| `candidate_tools` | 规划时在该工具集内考虑过的工具 |
| `arguments` | 规划阶段就已确定的实参（仍需过参数编译器） |
| `depends_on` | 依赖的任务 id（逻辑先后） |
| `resources` | 占用的资源锁名（物理冲突） |
| `confidence` | 该任务的置信度 |

**两种形态的保证强度不同**，这是选型时最需要知道的一点：

| 保证 | 分层规划（默认） | 整份规划 |
| --- | --- | --- |
| 候选集不逃逸 | 每层选择当场对着候选集校验，逃逸即判错 | 事后校验：工具 / 工具集 / 大类的父子关系必须与树一致 |
| 原文依据 | 要求逐字取自原句；定位不到时仅记为无位置，不阻断 | **强校验**：`evidence` 必须是原句连续子串，模型给的 offset 只作参考、对不上由 Harness 确定性重定位，定位不到即整份判非法 |
| 候选工具合法性 | 候选集由树直接给出，天然合法 | 校验 `candidate_tools` 全部落在该工具集内 |
| 置信度范围 | 逐层取值，整体取最小 | 校验必须在 `[0,1]` |
| 依赖图 | 只从"然后 / 再 / 接着"这类明确先后词生成依赖，并列意图相互独立 | **完整图校验**：id 唯一、不得自依赖、不得引用未知 id、**不得有环** |
| 资源锁 | **不产出**——当前分层规划不填 `resources`，并发冲突需靠依赖表达 | 产出：任务未声明时按工具的部件标识或所属工具集派生默认锁名 |

要求 `evidence` 逐字来自原句，是并行防幻觉的关键：一个任务如果在原话里找不到对应的字，它就不该存在。这条在整份规划里是硬校验；分层规划目前只做定位、不阻断，需要补齐（见 §3 A8）。

**规划失败不抛异常**：分层规划任一步出错时返回一份带澄清标记与原因的空计划，由调度层统一转成如实的失败回执，不会让整个连接断掉。

**调度**：

| 机制 | 说明 |
| --- | --- |
| 就绪批次 | 依赖全部 `Done`、且资源锁互不冲突的任务组成一批 |
| 批内并发 / 批间串行 | 一批同时跑，全批结束再算下一批 |
| 依赖失败传播 | 前序失败，后继直接判 `dependency_failed`，不进批 |
| 图阻塞 | 还有待办任务却凑不出就绪批次时，直接判定依赖图阻塞并报错，不空转 |
| 打断 | 取消整批在跑的任务，所有未完成任务标 `interrupted`，状态如实广播 |
| Schema 槽位 | 按任务 id 分槽，每个并发任务持有自己那一份，用完各自释放 |

并行模式下**没有 rules 播报通道**：批次结束后统一组装上下文交给模型播报。rules 直答目前只存在于串行模式。

#### 2.11.3 任务状态机

两种模式共用同一套状态：

> 《任务状态机图｜配图待补 —— 由图示负责人绘制》
>
> 图需表达：Todo → Doing（串行取得单任务锁 / 并行进入就绪批次）→ Done（回执 success）/ Failed（回执 failure、重试耗尽、被打断、前序依赖失败）/ Waiting（需要澄清，回答回填后转回 Doing，超时或用户改主意转 Failed）。

| 不变量 | 说明 |
| --- | --- |
| 并发受控 | 串行下 Doing 唯一；并行下 Doing 集合内资源锁两两不冲突 |
| 挂起不占位 | 任务转 `Waiting` 等待澄清时让出执行位，后续任务可以先跑，避免一句没问清卡死整轮 |
| 重试有上限 | 失败任务按配置次数重试，耗尽即终态，不无限重试拖死整轮 |
| 全量可见 | 每次状态变化都把**全量任务列表**广播给前端，也写回上下文；前端不自己维护任务状态 |

### 2.12 交互协议与事件帧

下行统一信封：`{type, payload, sequence, timestamp_ms, turn_id}`。`sequence` 会话内单调递增，客户端据此判乱序与丢帧；`turn_id` 在每次打断后换新，客户端可以据此直接丢弃上一轮的残留帧。

| 阶段 | 事件 |
| --- | --- |
| 会话 | `session.ready`（会话 id、上下行音频格式与采样率） |
| 轮次 | `turn.started` / `turn.end` / `turn.interrupted` |
| 识别 | `transcript.partial` / `transcript.final` |
| 判停 | 判停器状态帧 |
| 分类 | `classification.level`（逐层一条，带候选数与置信度）/ `classification.path`（整条路径）/ `agenda.classification`（多意图下每个任务各一条） |
| 任务 | `agenda.state`（全量任务列表） |
| 工具 | `tool.receipt` |
| 子代理 | `subagent.response`（计划 + 摘要） |
| 生成 | `llm.delta` / `llm.done`；rules 直答走 `response.rule`，其增量带 `source: rules` 标记 |
| 语音 | `tts.audio.delta`（PCM16LE / 24 kHz）/ `tts.done` |
| 延迟 | `latency`（`first_token` / `first_audio`，每条自带 `from` 字段标明锚点为 `turn.end`） |
| 异常 | `error`（带 `stage` 与 `recoverable`，可恢复的错误不断连接） |

逐层发 `classification.level` 而不是只发最终结果，是为了让"错在哪一层"当场可见——上层选错，下层再准也回不来。

### 2.13 关键数据契约

**分类树**：三层结构，节点带 `id / label / description / 父节点 id`；顶层带完整性块（工具总数、是否规范完整、内容哈希）。当前目录规模为 **546 个工具 / 96 个工具集 / 10 个大类**，其中 1 个大类下没有任何工具集，构造候选集时被剔除，实际可选 **9 个**。工具集规模长尾明显：平均每个工具集 5.7 个工具，最大的一个有 **73** 个，另有 **32** 个工具集只含单个工具——末层候选集长度极不均衡，是判准的主要风险来源。

**工具 Schema**（每工具一份独立文件）：

| 字段 | 用途 |
| --- | --- |
| `tool_id` | 与索引互校，防止张冠李戴 |
| `properties` | 参数名 → 类型 / 枚举 / 取值域 |
| `required` | 必填参数，缺失即拒绝执行 |
| `x-dangerous` | 危险动作，需用户显式确认才放行 |
| `x-min-confidence` | 该工具要求的最低分类置信度 |
| `x-aliases` | 参数别名，供归一化使用 |

**任务对象**：两种模式共用的基础字段是 `id / query / status / attempts / depends_on / tool_id / result / error`，外加一条纠正痕迹列表。并行模式的任务额外携带规划信息：`resources / category / toolset / arguments / confidence / evidence / support_span / candidate_tools / class_path`。

**工具回执**：`{task_id, success, payload, error}` —— 判断执行是否成功的**唯一**依据。


## 3. 开发模块拆解

### 3.1 模块清单

**A · 主链路**

| # | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A1 | 工具目录标注补齐 | 为全量 Schema 补齐 `required` / `x-dangerous` / `x-min-confidence` / `x-aliases` | 目录源数据 | P0 | 待启动 |
| A2 | Intent Head | 训练、导出、侧车推理接入 | 标注数据、目录版本 | P0 | 进行中 |
| A2b | ASR Head | | | | 待定 |
| A3 | 分类树构建与校验 | 目录 → 三层分类树流水线 + 完整性校验；运行时源文件哈希比对 | A1 | P0 | 进行中 |
| A4 | 全双工前端 | 判停、打断门控、延迟锚点埋点 | —— | P0 | 进行中 |
| A5 | 单意图主链路 | 三道分流 + 单份 Schema 装载 + 参数编译 + 执行 + 播报分流 | A2 / A3 | P0 | 进行中 |
| A6 | 流式 TTS | 增量断句 + 流式音频 + 打断即停 | A4 | P1 | 进行中 |
| A6b | 确定性纠正栈 | 同工具集内的锚点纠正与候选补全、可审计痕迹；目前默认关闭，需评估后决定是否默认开启 | A5 | P2 | 进行中 |
| A7 | 多意图·串行调度 | 切割、状态机、单任务锁、依赖传播、重试 | A5 | P1 | 进行中 |
| A8 | 多意图·并行调度 | 分层规划与整份规划双形态、计划校验、DAG、资源锁、批次执行、分槽装载；分层规划的原文依据强校验与资源锁产出待补齐 | A5 | P1 | 进行中 |
| A9 | Subagent 通道 | 子代理任务协议、结果回填、与工具道的边界 | A5 | P1 | 待细化 |

**B · 记忆与上下文**

| # | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B1 | 会话记忆 | | | | 待定 |
| B2 | 上下文预算与压缩 | | | | 待定 |
| B3 | 长期记忆 | | | | 待定 |
| B4 | 会话持久化 | | | | 待定 |

**C · 交互与安全**

| # | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C1 | 澄清回合 | | | | 待定 |
| C2 | 危险动作确认交互 | | | | 待定 |
| C3 | 车辆状态接入 | | | | 待定 |
| C4 | 说话人与座位归属 | | | | 待定 |
| C5 | 响应侧校验 | | | | 待定 |

**D · 工程支撑**

| # | 模块 | 交付内容 | 依赖 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| D1 | 前端事件协议 | 事件帧定义、序号与乱序处理、客户端联调 | A4 / A5 | P1 | 进行中 |
| D2 | 可观测性 | | | | 待定 |
| D3 | 外部服务韧性 | | | | 待定 |
| D4 | 弱网与离线降级 | | | | 待定 |
| D5 | 沙箱 | | | | 暂时还没有规划这个 |
| D6 | 评测 | | | | 待定 |

### 3.2 需要优先拉通的三件事

1. **A1 卡住 A5 的参数防线**。当前 546 份 Schema 里带 `required`、`x-dangerous`、`x-min-confidence`、`x-aliases` 的**各为 0 份**——必填校验与危险动作确认两道闸永远不触发，置信度门限退化成一个全局默认值，别名归一无从生效。能真正跑起来的只有类型强转与枚举校验（794 个参数带枚举）。标注不补齐，这几条防线等于没有。
2. **A2 与 A3 必须共享同一份目录版本**。Head 的输出空间就是分类树的候选集，两边版本一旦不同步，模型会输出树上不存在的 id，全部落成硬错误。目录源文件哈希已经记录在完整性块里，但运行时还没有比对，错版目前发现不了。
3. **A4 的延迟口径要先定死**。首字、首音两个里程碑必须以判停时刻为统一锚点，否则后续所有优化都没有可比基线。

### 3.3 风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 分类树标签歧义 | 相邻大类语义重叠，模型在上层选错就再也回不来 | 标签与描述去歧义；上层低置信转澄清 |
| 目录规模膨胀 | 单个工具集下工具过多时，末层候选集过长影响判准 | 对超大工具集做二次拆分 |
| 并行规划整份作废 | 计划里任何一条校验不过，整份计划判非法，多意图整轮失败 | 校验失败时回落串行切割，不让一个坏任务拖垮整句 |
| 串行链路延迟累积 | 多意图任务数越多，末尾任务等待越久 | 成功工具链走 rules 播报省一次往返；意图彼此独立时切并行调度 |
| 外部工具不可用 | 无回执可用 | 明确降级为失败并如实播报，禁止伪造成功 |
| 长期记忆误记 | 把一次性操作当成稳定偏好，之后每次都用错的默认值 | 只在明确表达或反复确认后写入；用户可查可删 |
| 澄清打断节奏 | 追问太频繁会让全双工体验变得比按键还慢 | 只在三类确定性原因下追问；能用默认值补的不问 |
